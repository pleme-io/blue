# Bidama catalogue

Generated by `mokuroku` from 28 packages and 1445 definitions. Do not edit by hand; regenerate it, and let the freshness check tell you when it is stale.

| package | version | needs | tests | defs | what it is |
|---|---|---|---|---|---|
| [`angou`](#angou) | 0.1.0 | retsu | 22 | 60 | angou (暗号) — number theory and the classical ciphers built on it. |
| [`deeta`](#deeta) | 0.1.0 | — | 7 | 5 | deeta (データ) — JSON data, read safely. |
| [`gyouretsu`](#gyouretsu) | 0.1.0 | retsu, kazu | 27 | 50 | gyouretsu (行列) — vectors and matrices as nested lists. |
| [`hizuke`](#hizuke) | 0.1.0 | retsu | 33 | 46 | hizuke (日付) — the proleptic Gregorian calendar, as arithmetic. |
| [`junjo`](#junjo) | 0.1.0 | ronri | 27 | 37 | junjo (順序) — ordering: sorting, searching, selection. |
| [`kansuu`](#kansuu) | 0.1.0 | — | 19 | 24 | kansuu (関数) — functions about functions. |
| [`kazu`](#kazu) | 0.1.0 | — | 22 | 33 | kazu (数) — numbers. |
| [`kikagaku`](#kikagaku) | 0.1.0 | retsu, kazu | 28 | 62 | kikagaku (幾何学) — plane geometry on [x, y] points. |
| [`kueri`](#kueri) | 0.1.0 | retsu, moji, shuugou, shisutemu, deeta | 18 | 164 | kueri (クエリ) — queries: analysis is authored in blue, and SQL is only the rendered bridge to DuckDB. |
| [`kumiawase`](#kumiawase) | 0.1.0 | kazu | 26 | 39 | kumiawase (組み合わせ) — counting without enumerating. |
| [`moji`](#moji) | 0.1.0 | retsu | 40 | 58 | moji (文字) — strings. |
| [`mokuroku`](#mokuroku) | 0.1.0 | retsu, junjo, shuugou | 10 | 30 | mokuroku (目録): the catalogue of what every bidama on a BLUE_PATH provides. |
| [`nisshi`](#nisshi) | 0.1.0 | retsu, shuugou, junjo, deeta, shomei, raifusaikuru | 10 | 143 | nisshi (日誌) — an event log: append-only JSON Lines, hash-chained, optionally signed, replayed into lifecycle states. |
| [`ongaku`](#ongaku) | 0.1.0 | retsu, kazu | 41 | 104 | ongaku (音楽) — twelve-tone music theory, as arithmetic on semitones. |
| [`raifusaikuru`](#raifusaikuru) | 0.1.0 | retsu, shuugou | 9 | 185 | raifusaikuru (ライフサイクル) — lifecycles: event-sourced states, guards that name the rule and the task, and permit limits. |
| [`ran`](#ran) | 0.1.0 | retsu, shuugou, junjo | 30 | 31 | ran (乱) — deterministic pseudo-randomness. |
| [`retsu`](#retsu) | 0.1.0 | kazu | 29 | 49 | retsu (列) — lists, made TOTAL. |
| [`ronri`](#ronri) | 0.1.0 | retsu, kansuu | 23 | 33 | ronri (論理) — predicates, and the algebra over them. |
| [`ryouiki`](#ryouiki) | 0.1.0 | retsu, junjo, toukei, ran | 4 | 11 | ryouiki (領域) — region: which region of the inputs produces an outcome. |
| [`sabi`](#sabi) | 0.1.0 | retsu | 6 | 42 | sabi (錆): Rust source from blue data. Build the items as values, render once. |
| [`seimei`](#seimei) | 0.1.0 | retsu | 31 | 48 | seimei (生命) — cellular automata: elementary rules, and Life. |
| [`shinsuu`](#shinsuu) | 0.1.0 | retsu | 28 | 80 | shinsuu (進数) — base conversion and checksums. |
| [`shisutemu`](#shisutemu) | 0.1.0 | retsu, moji | 9 | 12 | shisutemu (システム) — the system. |
| [`shomei`](#shomei) | 0.1.0 | retsu | 9 | 16 | shomei (署名) — signatures and hash chains: Ed25519 and BLAKE3, for real. |
| [`shuugou`](#shuugou) | 0.1.0 | ronri, retsu | 19 | 29 | shuugou (集合) — sets, over plain lists. |
| [`tokumei`](#tokumei) | 0.1.0 | ran, toukei, kazu | 7 | 10 | tokumei (匿名) — anonymity: learn what a group is without exposing anyone in it. |
| [`toukei`](#toukei) | 0.1.0 | junjo, shuugou | 30 | 44 | toukei (統計) — descriptive statistics. |
| [`zenbu`](#zenbu) | 0.1.0 | angou, deeta, gyouretsu, hizuke, junjo, kansuu, kazu, kikagaku, kueri, kumiawase, moji, mokuroku, nisshi, ongaku, raifusaikuru, ran, retsu, ronri, ryouiki, sabi, seimei, shinsuu, shisutemu, shomei, shuugou, tokumei, toukei | 2 | 0 | zenbu (全部) — the whole distribution, as ONE dependency. |

## angou

`angou` 0.1.0 · needs: retsu

angou (暗号) — number theory and the classical ciphers built on it.

| definition | what it does |
|---|---|
| `is_prime(n)` |  |
| `no_factor_from(n, k)` |  |
| `primes_below(n)` |  |
| `prime_factors(n)` |  |
| `factor_from(n, k)` |  |
| `mod_pow(base, e, m)` | Modular exponentiation by repeated squaring. |
| `totient(n)` | Euler's totient: how many integers below n are coprime to it. |
| `sieve(n)` | The sieve of Eratosthenes, in the only shape blue can express it. |
| `sieve_from(xs)` |  |
| `nth_prime(k)` | 1-indexed, because "the first prime" is 2 and nobody means the zeroth. |
| `nth_prime_from(k, c)` |  |
| `next_prime(n)` | Strictly greater than n, so next_prime(7) is 11 and not 7 — otherwise |
| `next_prime_from(c)` |  |
| `is_coprime(a, b)` |  |
| `extended_gcd(a, b)` | Bezout: returns [g, x, y] with a*x + b*y == g and g == gcd(a, b). |
| `mod_inverse(a, m)` | The inverse of a mod m, or 0-1 when there is none. An inverse exists exactly |
| `crt2(a1, n1, a2, n2)` | Chinese remainder for exactly two congruences: the smallest non-negative x |
| `divisors(n)` | Every divisor of n, ascending, n included. Trial division over the whole |
| `proper_divisors(n)` |  |
| `sum_of_divisors(n)` | sigma(n), counting n itself — the convention every table uses. |
| `is_perfect(n)` | Perfect: equal to the sum of its PROPER divisors, which is sigma(n) == 2n. |
| `is_abundant(n)` |  |
| `is_deficient(n)` |  |
| `multiplicative_order(a, n)` | The smallest m >= 1 with a^m == 1 mod n, or 0-1 when a is not invertible. |
| `order_from(a, n, m, cur)` |  |
| `carmichael_lambda(n)` | Carmichael's lambda: the exponent of the group of units mod n, i.e. the |
| `fermat_probable_prime(n, a)` | Fermat's test to base a. A `false` is a PROOF that n is composite; a `true` |
| `is_carmichael(n)` | A Carmichael number is composite and yet passes Fermat's test for every base |
| `primitive_root(p)` | The smallest generator of the units mod a prime p — where Diffie-Hellman |
| `alphabet()` |  |
| `letter_index(ch)` |  |
| `find_index_of(xs, v, i)` |  |
| `caesar(text, n)` | Shift each letter by n; anything that is not a lowercase letter passes |
| `shift_char(ch, n)` |  |
| `rot13(text)` |  |
| `vigenere(text, key)` | Vigenere: a caesar whose shift is driven by a repeating key. |
| `vigenere_decrypt(text, key)` |  |
| `vigenere_with(text, key, dir)` |  |
| `key_shifts(key)` | Non-letters in the KEY are dropped rather than treated as shift zero, so |
| `vigenere_chars(cs, ks, ki, dir)` |  |
| `atbash(text)` | Atbash reverses the alphabet: a<->z, b<->y. It has no key at all, so the |
| `atbash_char(ch)` |  |
| `rail_fence(text, rails)` | Rail fence: write the text down and up across `rails` lines, then read the |
| `rail_fence_decrypt(text, rails)` | Decryption is the inverse permutation. Building `rail_order` once and |
| `rail_order(n, rails)` | The positions of the original text, grouped by rail — i.e. cipher position |
| `rail_of(i, rails)` | The zigzag has period 2*rails-2: down to the bottom rail and back up, |
| `letter_counts(text)` | Counts of a..z in alphabet order, case-folded, ignoring everything that is |
| `letter_count(text, ch)` |  |
| `most_common_letter(text)` | Ties break toward the earlier letter, and a text with no letters at all |
| `substitution(text, key)` | A simple substitution cipher: `key` is a 26-letter permutation, so plaintext |
| `substitute_char(ch, ks)` |  |
| `substitution_decrypt(text, key)` |  |
| `invert_key(key)` | The inverse permutation, or "" when `key` is not one — which then makes |
| `is_substitution_key(key)` |  |
| `keyword_key(word)` | The classical way a human generated a substitution key without memorising |
| `dedupe_letters(cs, seen)` |  |
| `caesar_candidates(text)` | Every decryption of a caesar text, indexed by the shift that produced it — |
| `crack_caesar(text)` | The second half: you do not even have to look. Scoring each candidate |
| `english_score(text)` |  |
| `english_weights()` | English letter frequencies per 1000 characters, a..z, rounded to whole |

## deeta

`deeta` 0.1.0 · needs: —

deeta (データ) — JSON data, read safely.

| definition | what it does |
|---|---|
| `is_doc(v)` | True when `v` is a JSON object read for its fields. |
| `as_json(doc, key)` | The raw value at `key`, or nil when there is none to give. |
| `get_str(doc, key, default)` | The string at `key`, or `default` when the field is not a string. |
| `get_int(doc, key, default)` | The integer at `key`, or `default` when the field is not an integer. |
| `get_number(doc, key, default)` | The number at `key` — integer or float — or `default` when the field is not |

## gyouretsu

`gyouretsu` 0.1.0 · needs: retsu, kazu

gyouretsu (行列) — vectors and matrices as nested lists.

| definition | what it does |
|---|---|
| `dot(a, b)` |  |
| `scale(k, v)` |  |
| `vadd(a, b)` |  |
| `vsub(a, b)` |  |
| `magnitude(v)` |  |
| `is_zero_vector(v)` | Zero-length test by distance, never by `== 0`. |
| `vector_near(a, b)` | Element-wise approximate equality. Anything that has been through `/` or |
| `normalize(v)` |  |
| `vdistance(a, b)` | Distance between two vectors of any arity. Named `vdistance` rather than |
| `vdistance_squared(a, b)` | The same ordering without the square root — for sorting or comparing |
| `cross_product(a, b)` | The 3-D cross product only. There is no n-dimensional cross product, so this |
| `projection(a, b)` | Vector projection of a onto b. The denominator is dot(b, b) — using dot(a, a) |
| `arccos(x)` | Arc cosine by bisection, because this runtime has no `acos` — `sin`, `cos`, |
| `arccos_bisect(x, lo, hi, n)` |  |
| `angle_between(a, b)` | Angle in radians. A zero vector has no direction, so there is no angle to |
| `rows(m)` |  |
| `cols(m)` |  |
| `col(m, j)` |  |
| `transpose(m)` |  |
| `matmul(a, b)` |  |
| `identity_matrix(n)` |  |
| `shape(m)` | `rows`/`cols` above are the fast path and both go through `length`/`car`, |
| `row(m, i)` |  |
| `is_square(m)` |  |
| `matrix_equal(a, b)` | `==` already compares nested lists structurally, so this is not a re-spelling |
| `matrix_near(a, b)` | The one to reach for after a divide, a root or a solve — see vector_near. |
| `is_symmetric(m)` |  |
| `is_identity(m)` |  |
| `filled(r, c, v)` |  |
| `zeros(r, c)` |  |
| `madd(a, b)` |  |
| `msub(a, b)` |  |
| `mscale(k, m)` |  |
| `mvmul(m, v)` | Matrix times column vector. Written separately from `matmul` because wrapping |
| `outer_product(a, b)` | Outer product: an n-vector and an m-vector give an n-by-m rank-1 matrix. |
| `diagonal(m)` |  |
| `trace(m)` |  |
| `hadamard(a, b)` | Entrywise product, which is NOT matrix multiplication. It exists here under |
| `is_orthogonal(m)` | A matrix whose transpose is its inverse — rotations and reflections. Tested |
| `replace_col(m, j, v)` |  |
| `submatrix(m, i, j)` | The matrix with row i and column j struck out. `minor` is the DETERMINANT of |
| `cofactor_sign(i, j)` | The checkerboard sign (-1)^(i+j) that turns a minor into a cofactor. Getting |
| `determinant(m)` | Laplace expansion along the first row, so this is general rather than a pair |
| `minor(m, i, j)` |  |
| `cofactor(m, i, j)` |  |
| `adjugate(m)` | The transpose of the cofactor matrix — note the transpose, which is the step |
| `inverse(m)` | The empty list means "no inverse", not "the empty matrix": a singular matrix |
| `matrix_power(m, n)` | Non-negative powers only. A negative power is spelled |
| `solve_cramer(m, b)` | Cramer's rule for m * x = b, at any order. Returns [] when the system has no |
| `solve2x2(m, b)` | The 2-by-2 case by name, since it is the one that turns up by hand. |

## hizuke

`hizuke` 0.1.0 · needs: retsu

hizuke (日付) — the proleptic Gregorian calendar, as arithmetic.

| definition | what it does |
|---|---|
| `is_leap(y)` |  |
| `days_in_month(y, m)` |  |
| `days_in_year(y)` |  |
| `ordinal_day(y, m, d)` | Day of the year, 1-based. |
| `day_of_week(y, m, d)` | Day of week by Zeller's congruence: 0 = Sunday. |
| `is_weekend(y, m, d)` |  |
| `day_name(w)` |  |
| `to_day_number(y, m, d)` | A serial day number: 0001-01-01 is day 1, counting proleptic Gregorian days |
| `from_day_number(n)` | The exact inverse of to_day_number, as [year, month, day]. |
| `year_of(dt)` | Named accessors, so a caller never has to remember that month is index 1. |
| `month_of(dt)` |  |
| `day_of(dt)` |  |
| `days_between(y1, m1, d1, y2, m2, d2)` | Positive when the SECOND date is the later one, matching how "days between |
| `add_days(y, m, d, n)` |  |
| `next_day(y, m, d)` |  |
| `prev_day(y, m, d)` |  |
| `shift_days(dt, n)` | add_days for a date that is already a list — the form that chains, because |
| `month_name(m)` | Months are 1-based, matching every other function in this file, so the list |
| `day_of_week_name(y, m, d)` |  |
| `quarter(m)` |  |
| `is_valid_date(y, m, d)` | The guard that stops 2023-02-29 and 2024-13-01 from silently becoming a real |
| `is_month_end(y, m, d)` |  |
| `last_day_of_month(y, m)` | Returns a DATE, not a count — days_in_month already answers the count, and a |
| `first_day_of_month(y, m)` |  |
| `is_business_day(y, m, d)` |  |
| `weekday_of_day_number(n)` | 0001-01-01 was a Monday and day numbers have no gaps, so the day number mod 7 |
| `week_of_year(y, m, d)` | The plain "which week of this year" count: weeks start on Sunday and week 1 |
| `iso_weekday(y, m, d)` | ISO 8601 numbers weekdays 1..7 starting MONDAY, unlike day_of_week's 0=Sunday. |
| `iso_weeks_in_year(y)` | A 53-week ISO year is exactly one starting on Thursday, or a leap year |
| `iso_week_raw(y, m, d)` | The raw ISO week index, which may fall OFF either end of its own year: 0 or |
| `iso_week(y, m, d)` |  |
| `iso_week_year(y, m, d)` | The year an ISO week belongs to, which is NOT always the calendar year — the |
| `age_in_years(by, bm, bd, y, m, d)` | Completed years between a birth date and a given date. "Today" is an argument |
| `add_months(y, m, d, n)` | Adding a month is not adding 30 days, and the trap is the SHORT target month: |
| `add_years(y, m, d, n)` | The same clamp one dimension up: 29 February plus a year is 28 February. |
| `compare_dates(y1, m1, d1, y2, m2, d2)` | -1 / 0 / 1, so it can drive a sort. Comparing day numbers instead of |
| `business_days_in_span(a, b)` | Counts at the DAY-NUMBER level and tests the weekday with a single mod, |
| `business_days_between(y1, m1, d1, y2, m2, d2)` | Business days in the HALF-OPEN span [start, end) — start counted, end |
| `easter(y)` | Easter Sunday, by the anonymous Gregorian algorithm (Meeus/Jones/Butcher). |
| `date_from_ordinal(y, n)` | The inverse of ordinal_day. Day 60 is 29 February in 2024 and 1 March in |
| `nth_weekday_of_month(y, m, w, n)` | "The fourth Thursday of November" — how holiday and meeting rules are |
| `last_weekday_of_month(y, m, w)` | "The last Monday in May" — the other half of the holiday vocabulary, and the |
| `zero_pad(n, width)` | Left-pads with zeros to a fixed width, returning the number unchanged if it |
| `pad_to(s, width)` | The recursive worker: one zero per pass, so the width is a stop condition |
| `format_iso(y, m, d)` | ISO 8601 is FIXED WIDTH: "2024-02-09", never "2024-2-9". The padding is the |
| `format_iso_date(dt)` | The same, for a date that is already a list — so easter, add_days and the |

## junjo

`junjo` 0.1.0 · needs: ronri

junjo (順序) — ordering: sorting, searching, selection.

| definition | what it does |
|---|---|
| `insert_ordered(x, xs)` |  |
| `sort(xs)` |  |
| `sort_by(key, xs)` | NOT stable, unlike `sort` above — the header's stability claim covers the |
| `sort_desc(xs)` |  |
| `is_sorted(xs)` |  |
| `bsearch(xs, target, lo, hi)` |  |
| `index_of_sorted(xs, target)` | Binary search over a SORTED list. Returns the index, or -1. |
| `min_by(key, xs)` |  |
| `max_by(key, xs)` |  |
| `compare_values(a, b)` | The three-way comparison the rest of the package is phrased in terms of. |
| `compare_lists(a, b)` | Lexicographic order over lists: the first differing element decides, and a |
| `merge_sorted(a, b)` | Merge two ALREADY-SORTED lists into one sorted list. |
| `merge_sorted_by(key, a, b)` |  |
| `is_sorted_by(key, xs)` |  |
| `is_sorted_desc(xs)` |  |
| `sort_desc_by(key, xs)` |  |
| `insert_sorted_by(key, x, xs)` | Insert into a list already sorted BY KEY, before the first equal key. |
| `sort_stable_by(key, xs)` | A STABLE sort by key — equal keys come out in input order. |
| `merge_sort(xs)` | Merge sort: split, sort each half, merge. Also stable, since merge_sorted |
| `dedupe_sorted(xs)` | Drop ADJACENT duplicates. On a sorted list that is every duplicate; on an |
| `lower_bound_from(xs, target, lo, hi)` |  |
| `upper_bound_from(xs, target, lo, hi)` |  |
| `lower_bound(xs, target)` | The FIRST index at which target could be inserted without breaking the order. |
| `upper_bound(xs, target)` | The LAST such index — one past the final occurrence of target. |
| `count_sorted(xs, target)` | How many times target occurs, in O(log n) rather than a scan. |
| `contains_sorted(xs, target)` |  |
| `median_of_three(a, b, c)` | The median of three values, which is the standard cheap pivot choice. |
| `nth_smallest(n, xs)` | 0-indexed, to agree with `nth` rather than with English: nth_smallest(0, xs) |
| `nth_largest(n, xs)` |  |
| `top_n(n, xs)` | The n largest, largest first. Asking for more than there is yields everything |
| `bottom_n(n, xs)` |  |
| `argsort(xs)` | The indices of xs in sorted order: nth(nth(0, argsort(xs)), xs) is the |
| `rank(xs)` | Where each element LANDS in sorted order — the inverse permutation of |
| `is_strictly_sorted(xs)` | Sorted AND without duplicates. `is_sorted` says yes to [1, 1]; this does not, |
| `merge_all_sorted(xss)` | k-way merge: fold the pairwise merge over a list of sorted lists. |
| `insert_lex(x, xss)` |  |
| `sort_lists(xss)` | Sort a list OF LISTS lexicographically. |

## kansuu

`kansuu` 0.1.0 · needs: —

kansuu (関数) — functions about functions.

| definition | what it does |
|---|---|
| `identity(x)` |  |
| `constantly(x)` | The constant function. Useful wherever a callback is required and ignored. |
| `compose(f, g)` | compose(f, g) applies g FIRST, then f — the mathematical order. |
| `pipe(f, g)` | pipe(f, g) applies f FIRST — reading order. |
| `flip(f)` | flip a two-argument function's arguments. |
| `iterate_n(f, x, n)` | Apply f to x, n times. |
| `compose3(f, g, h)` | Three-place versions of the two orders. Written out rather than expressed as |
| `pipe3(f, g, h)` |  |
| `pipe_all(fs)` | pipe_all folds a LIST of functions into one, left to right. |
| `compose_all(fs)` | The mathematical order: compose_all([f, g, h]) is f∘g∘h, so h runs first. |
| `curry2(f)` | curry2 turns f(a, b) into f(a)(b), so the first argument can be fixed and the |
| `uncurry2(g)` |  |
| `apply_twice(f, x)` | f(f(x)). The general form is iterate_n; this exists because doubling up is |
| `iterated(f, n)` | The combinator form of iterate_n: n is fixed, the value is not. |
| `on(op, key)` | Haskell's `on`: compare/combine two values THROUGH a key function. |
| `juxt(fs)` | juxt(fs) applies EVERY function to ONE value and collects the results. |
| `both_results(f, g)` | The two-function case, named, because a pair is what most callers want and |
| `fork(op, f, g)` | fork is both_results with the pair already combined: op(f(x), g(x)). |
| `tap(f)` | tap runs f for its effect and hands the ORIGINAL value on. |
| `apply_when(p, f)` | Apply f only where p holds; elsewhere pass the value through unchanged. |
| `is_fixed_point(f, x)` |  |
| `until_stable(f, x, max_steps)` | Apply f until the value stops changing, or until max_steps applications. |
| `converges(f, x, max_steps)` | Did it actually settle within the bound? The question until_stable's return |
| `stable_steps(f, x, max_steps)` | How many applications until stable; max_steps if it never got there. |

## kazu

`kazu` 0.1.0 · needs: —

kazu (数) — numbers.

| definition | what it does |
|---|---|
| `abs(n)` |  |
| `sign(n)` |  |
| `max(a, b)` |  |
| `min(a, b)` |  |
| `clamp(n, lo, hi)` | Clamp into [lo, hi]. Named `clamp` rather than `bound` because every |
| `pow(base, exp)` |  |
| `even(n)` |  |
| `near(a, b)` | Approximate equality, for the float results `sqrt` and friends return. |
| `near_within(a, b, eps)` |  |
| `odd(n)` | Odd via the remainder rather than `!even(n)`, because blue's `%` is Euclidean |
| `is_zero(n)` | Zero by ordering, NOT by `n == 0`. |
| `is_positive(n)` |  |
| `is_negative(n)` |  |
| `in_range(n, lo, hi)` | Inclusive on both ends, matching clamp: whatever clamp returns is in_range. |
| `divides(d, n)` | Zero divides nothing — the guard exists because `n % 0` is a runtime error, |
| `floor_div(a, b)` | Division that rounds toward negative infinity, not toward zero. |
| `ceil_div(a, b)` |  |
| `mod_positive(a, m)` | A remainder guaranteed to land in [0, abs(m)). |
| `square(n)` |  |
| `cube(n)` |  |
| `int_sqrt(n)` | Integer square root: the largest r with r * r <= n. |
| `int_sqrt_fix(n, r)` |  |
| `digit_count(n)` | How many decimal digits the number is written with, sign ignored. |
| `sum(xs)` |  |
| `product(xs)` | Seeded with 1, not 0 — the empty product is one, and a 0 seed makes every |
| `min_of(xs)` | nil seeds the fold so that no sentinel can be mistaken for data. Seeding |
| `max_of(xs)` |  |
| `gcd_of(xs)` | The identity elements are the whole reason these read the way they do: |
| `lcm_of(xs)` |  |
| `lerp(a, b, t)` | Linear interpolation, written `a + (b - a) * t` rather than |
| `round_to(n, places)` | Round to a number of decimal places. `places` is a count, so 0 or more; |
| `percent(part, whole)` | `part` expressed as a percentage of `whole`. |
| `percent_change(from, to)` | Percentage change from one value to another. The denominator is the value |

## kikagaku

`kikagaku` 0.1.0 · needs: retsu, kazu

kikagaku (幾何学) — plane geometry on [x, y] points.

| definition | what it does |
|---|---|
| `px(p)` |  |
| `py(p)` |  |
| `distance_squared(a, b)` |  |
| `distance(a, b)` |  |
| `manhattan(a, b)` |  |
| `midpoint(a, b)` |  |
| `cross(o, a, b)` | Twice the signed area — positive for counter-clockwise winding. |
| `polygon_area(pts)` | Shoelace formula: works for any simple polygon, convex or not. |
| `perimeter(pts)` |  |
| `triangle_area(a, b, c)` |  |
| `collinear(a, b, c)` |  |
| `in_circle(p, centre, r)` |  |
| `circle_area(r)` |  |
| `pi()` |  |
| `dot_at(o, a, b)` | Dot product of the two vectors leaving `o`. The sign alone answers "is the |
| `orientation(a, b, c)` | 1 counter-clockwise, -1 clockwise, 0 collinear. The sign of `cross` and |
| `signed_area(pts)` | Shoelace WITHOUT the abs. `polygon_area` throws the sign away; the winding it |
| `winding(pts)` | 1 if the vertices run counter-clockwise, -1 clockwise, 0 for zero area. |
| `slope(a, b)` | nil for a vertical line: it has no slope, and dividing would raise rather than |
| `line_through(a, b)` | The line through a and b as [A, B, C] of Ax + By + C = 0. |
| `line_value(line, p)` | Ax + By + C at p: zero on the line, and its sign tells which side p is on. |
| `on_line(line, p)` |  |
| `point_line_distance(p, a, b)` | Perpendicular distance from p to the INFINITE line ab. Degenerate ab (a == b) |
| `closest_point_on_segment(p, a, b)` | The point of segment ab nearest p — clamped to the segment, which is what |
| `point_segment_distance(p, a, b)` |  |
| `bounding_box(pts)` | [[min x, min y], [max x, max y]], or nil for no points at all — an empty set |
| `point_in_rect(p, rect)` | Inclusive on the boundary, and the two corners may arrive in either order — |
| `circle_circumference(r)` |  |
| `radians(deg)` |  |
| `degrees(rad)` |  |
| `arc_length(r, theta)` | theta in RADIANS throughout, like sin/cos — a package that mixed the two |
| `sector_area(r, theta)` |  |
| `chord_length(r, theta)` |  |
| `arc_cosine(v)` | The inverse of cos, by bisection on cos, which is monotone across [0, pi]. |
| `arc_cosine_between(v, lo, hi, fuel)` |  |
| `angle_at(o, a, b)` | The angle at vertex `o` between the arms oa and ob, in radians, always in |
| `is_right_triangle(a, b, c)` | Compared on SQUARED lengths so integer input never goes through sqrt: a 3-4-5 |
| `triangle_kind(a, b, c)` | "equilateral" / "isosceles" / "scalene", plus "degenerate" for three points on |
| `vertex_centroid(pts)` | The mean of the vertices. Cheap, total, and NOT the centroid of the shape — |
| `shoelace_term(pts, i)` |  |
| `centroid(pts)` | The AREA centroid — the balance point of the enclosed region. |
| `is_convex(pts)` | Every turn the same way round. Collinear triples are dropped rather than |
| `crosses_ray(p, a, b)` | Does a rightward ray from p cross edge ab? The straddle test comes first, and |
| `crossings_from(p, pts, i, n, inside)` |  |
| `point_in_polygon(p, pts)` | Ray casting: an odd number of edge crossings means inside. |
| `point_on_polygon(p, pts)` |  |
| `rotate_point(p, theta)` |  |
| `rotate_point_about(p, centre, theta)` | Rotation about an arbitrary centre, expressed as translate-rotate-translate |
| `translate_polygon(pts, dx, dy)` | The whole-shape transforms carry `_polygon` rather than the bare `translate` |
| `scale_polygon(pts, k)` | Scaling about the ORIGIN, which moves the shape as well as resizing it. Use |
| `scale_polygon_about(pts, centre, k)` |  |
| `rotate_polygon(pts, theta)` |  |
| `hull_start(pts)` | The lowest point, ties broken leftmost. It is provably on the hull, which is |
| `hull_next(p, pts)` | The next hull vertex after p: the one that leaves every other point on its |
| `hull_walk(pts, p, acc, fuel)` |  |
| `convex_hull(pts)` | Convex hull by gift wrapping (Jarvis march), counter-clockwise from the lowest |
| `segments_intersect(p1, p2, p3, p4)` | Do the two closed segments share at least one point? |
| `segment_intersection(p1, p2, p3, p4)` | The single point two segments cross at, or nil. |
| `circumcenter(a, b, c)` | The centre of the circle through all three points, or nil when they are |
| `circumradius(a, b, c)` |  |
| `nearest_point(p, pts)` | The member of pts closest to p, or nil for no candidates. Seeded with nil so |
| `interior_angles(pts)` | The interior angle at each vertex, in order, in radians. |

## kueri

`kueri` 0.1.0 · needs: retsu, moji, shuugou, shisutemu, deeta

kueri (クエリ) — queries: analysis is authored in blue, and SQL is only the rendered bridge to DuckDB.

| definition | what it does |
|---|---|
| `q_name(x)` | A name as text: a keyword's name, or the string itself. |
| `q_reserved_words()` | Words an engine will not read as a bare identifier: SQL's reserved words and |
| `q_bare_ident?(s)` | Lower-case ASCII, digits and underscore, not starting with a digit, and not |
| `q_ident(name)` | An identifier as SQL: bare when safe, otherwise double-quoted with any |
| `q_quote_str(s)` | A string value as a SQL literal: single quotes, embedded quotes doubled. |
| `q_float_text(x)` | A float's digits, always with a decimal point: to_s(1.0) is "1", which an |
| `q_types()` | The closed set of column types a contract or a source may declare. |
| `q_type(t)` |  |
| `q_type_sql(t, dialect)` | A type's name in a dialect. DOUBLE is DuckDB's; the standard spells it |
| `q_col(name, t)` | A declared column: a source's input or a model's contract. |
| `q_col_names(cols)` |  |
| `q_c(name)` | A column reference. |
| `q_int(n)` |  |
| `q_double(x)` | A DOUBLE literal, typed on purpose: DuckDB reads a bare 1.0 as DECIMAL(2,1). |
| `q_str(s)` |  |
| `q_bool(b)` |  |
| `q_null()` |  |
| `q_value?(x)` |  |
| `q_lit(v)` | A blue value as a literal, typed by what it is: an int stays an INTEGER and a |
| `q_expr(x)` | Anything in expression position: a keyword is a column, a value is a |
| `q_op(op, prec, a, b)` | A binary operator. `prec` orders them for the renderer's parentheses: |
| `q_add(a, b)` |  |
| `q_sub(a, b)` |  |
| `q_mul(a, b)` |  |
| `q_div(a, b)` |  |
| `q_eq(a, b)` |  |
| `q_ne(a, b)` |  |
| `q_lt(a, b)` |  |
| `q_le(a, b)` |  |
| `q_gt(a, b)` |  |
| `q_ge(a, b)` |  |
| `q_and(a, b)` |  |
| `q_or(a, b)` |  |
| `q_not(a)` |  |
| `q_round(x, digits)` |  |
| `q_cast(x, t)` |  |
| `q_agg_fn(f, args)` |  |
| `q_min(x)` |  |
| `q_max(x)` |  |
| `q_sum(x)` |  |
| `q_avg(x)` |  |
| `q_count(x)` |  |
| `q_count_all()` | count(*). |
| `q_string_agg(x, sep)` | Joins the values with `sep`. Order-sensitive, and with no core form: MySQL, |
| `q_row_number(partition, order)` | row_number() OVER (PARTITION BY … ORDER BY …). |
| `q_filtered(agg, pred)` | An aggregate over only the rows where `pred` holds: FILTER (WHERE …) in |
| `q_ordered(node, keys)` | An order for an order-sensitive aggregate or window. |
| `q_order_sensitive_fns()` | Functions whose result depends on the order rows arrive in. |
| `q_duckdb_only_fns()` | Functions with no core form. |
| `q_key(k)` |  |
| `q_desc(name)` |  |
| `q_key_names(keys)` |  |
| `q_source(opts)` | A delimited input file with DECLARED columns, in file order: read_csv maps |
| `q_raw(opts)` | The escape hatch: hand-written SQL, which must declare the columns it |
| `q_ref(x)` | A reference to an upstream source or model. The DAG is these nodes. |
| `q_rel(x)` | A relation in from/join position: a source or model is referenced, a ref or |
| `q_rel_name(rel)` |  |
| `q_reach_words()` |  |
| `q_rigor_words()` |  |
| `q_posture_of(words)` | The posture a model declares, as {reach, rigor}. Unknown words and two words |
| `q_reach_dialect(reach)` | The dialect a reach allows: :portable holds a model to :core. |
| `q_model(opts)` | A model is one SELECT with a name. opts: name, from, pipeline, posture, |
| `q_then(model, stages)` | Composition by data: the same model with more stages after its own. |
| `q_as(name, expr)` | A named output: a select item or a group aggregate. |
| `q_agg(name, expr)` |  |
| `q_agg_where(name, agg, pred)` | An aggregate over only the rows where `pred` holds, named. |
| `q_item(i)` |  |
| `q_select(items)` | Keep these columns (keywords) and computed ones (q_as), in this order. |
| `q_derive(name, expr)` | Add one computed column; every row stays. |
| `q_filter(pred)` | Keep the rows where `pred` holds. |
| `q_group(keys, aggs)` | One row per distinct key; `aggs` are q_agg / q_agg_where outputs. |
| `q_join(rel, keys)` | JOIN … USING (keys): the keys appear once, then the left's other columns, |
| `q_left_join(rel, keys)` |  |
| `q_sort(keys)` |  |
| `q_limit(n)` |  |
| `q_stage_exprs(stage)` |  |
| `q_label(i, stage)` |  |
| `q_kids(e)` |  |
| `q_nodes(e)` | Every node of an expression, the root first. |
| `q_nodes_of(e, kind)` |  |
| `q_node_refs(n)` |  |
| `q_refs(e)` | The column names an expression reads. |
| `q_outside_aggs(e)` |  |
| `q_bare_refs(e)` | The column names read OUTSIDE any aggregate — in a group, each must be a key. |
| `q_has?(e, kind)` |  |
| `q_nested_agg?(e)` |  |
| `q_refusal_kind(r)` |  |
| `q_refusal_why(r)` |  |
| `q_refusal_kinds(model)` |  |
| `q_unknown_refusals(names, cols, where)` |  |
| `q_dup_refusals(names, where)` |  |
| `q_expr_refusals(e, where, aggs_allowed, windows_allowed)` | What no expression may hold outside a group: an aggregate. And the order |
| `q_group_item_refusals(item, keys, where)` |  |
| `q_stage_refusals(stage, cols, where, next_kind)` |  |
| `q_join_refusals(stage, cols, where)` |  |
| `q_stage_output(stage, cols)` | The columns a stage returns, or nil when its input's are unknown. |
| `q_next_kind(stages, i)` |  |
| `q_walk(model)` | Thread the schema through the pipeline: {cols, refusals}. |
| `q_walk_stage(acc, i, stage, next_kind)` |  |
| `q_output(model)` | The column names a model returns, inferred through its pipeline; nil when an |
| `q_model_columns(model)` | What a downstream model sees: the contract when declared, the inference |
| `q_rel_columns(rel)` |  |
| `q_duckdb_uses(model)` | Constructs with no core form, as [what, where]. |
| `q_stage_uses(stage, where)` |  |
| `q_rel_uses(rel, where)` |  |
| `q_reach_refusals(model)` |  |
| `q_contract_refusals(model, out)` |  |
| `q_locked_refusals(model, out)` |  |
| `q_locked_limit(stages, i, stage)` |  |
| `q_refusals(model)` | Every violation, as data: [kind, why]. Empty when the model holds. |
| `q_refuse(refusals)` |  |
| `q_check(model)` | The model, or a thrown error naming every violation (its kind is the first's). |
| `q_dialect_refusals(model, dialect)` |  |
| `q_shift(model)` | blue shift's two questions for a model: the lowest reach that holds it, and |
| `q_block(from)` |  |
| `q_derived_names(b)` |  |
| `q_fits?(b, stage, dialect)` |  |
| `q_set(b, key, value, phase)` |  |
| `q_pushable?(b, stage)` | A filter that can join the WHERE of a block whose select list holds only |
| `q_subst(e, items)` | A filter after a group reads the aggregates by name; HAVING needs them |
| `q_into(b, stage, dialect)` |  |
| `q_lower_stage(acc, stage, dialect)` |  |
| `q_lower(model, dialect)` | The pipeline as closed CTE blocks ([name, block] pairs) and a final block. |
| `q_lock_order(model, b, dialect)` | A locked model's output order is made total: its own keys, then every other |
| `q_prec(e)` |  |
| `q_operand(child, parent_prec, tie_wraps, dialect)` | A child in operator position. Lower precedence is parenthesized; so is equal |
| `q_render_lit(e, dialect)` |  |
| `q_render_key(k)` |  |
| `q_order_suffix(keys)` |  |
| `q_render_agg(e, dialect)` | An aggregate. The FILTER-where form is DuckDB's; core moves the predicate |
| `q_render_win(e, dialect)` |  |
| `q_render_expr(e, dialect)` | One expression, in one dialect. The only place an expression becomes text. |
| `q_render_item(item, dialect)` |  |
| `q_render_conj(preds, dialect)` |  |
| `q_select_texts(b, dialect)` |  |
| `q_list_lines(items)` | `  item,` per line, the last without its comma. |
| `q_clause(word, preds, dialect)` |  |
| `q_join_line(j)` |  |
| `q_block_lines(b, dialect)` | One SELECT, as lines, clauses in SQL's order. |
| `q_indent(lines)` |  |
| `q_raws(model)` |  |
| `q_render_raw(r, dialect)` |  |
| `q_render_query(model, dialect)` | The query text, unchecked: q_render is the door that checks first. |
| `q_render(model, dialect)` | A model as one SQL query in `dialect` (:core or :duckdb), with no trailing |
| `q_delim_sql(d)` |  |
| `q_render_load(source, path, dialect)` | A view that reads a source's file with its declared columns. DuckDB only: |
| `q_materialize_target(model)` | Where a model's materialization lands by default: <name>.parquet or .csv. |
| `q_render_materialize(model, dialect, target)` | The statement that writes a model out: COPY … TO a zstd Parquet or a headed |
| `q_render_file(model, dialect, source)` | The file a consumer commits: a @generated header naming the blue program, |
| `q_write_sql(path, model, dialect, source)` | Write the rendered file; returns the path. |
| `q_rels(model)` |  |
| `q_targets(model)` |  |
| `q_deps(model)` | The names a model reads directly, first-seen order. Raw nodes are opaque and |
| `q_closure(model)` | Every upstream source and model, each before anything that reads it. |
| `q_closure_of(t)` |  |
| `q_sources(model)` |  |
| `q_duckdb(sql)` | Run SQL through the `duckdb` binary on PATH: [:ok, rows] or [:error, stderr]. |
| `q_duckdb_failed?(result)` |  |
| `q_row_values(result, names)` | The rows of a result as value lists, columns in `names` order; [] when it |
| `q_example_horizons()` | Worked example: per relationship length, the promise-keeping rate with and |
| `q_example_leverage()` | Worked example: how much full enforcement lifts keeping, per length. Every |
| `q_example_curve_cells()` | Worked example: which cell ran at which horizon and enforcement. |
| `q_example_curve_keep()` | Worked example: each cell's replicate mean, min and max of keeping. |
| `q_example_tipping()` | Worked example: the lowest enforcement at which a horizon keeps at least 90% |

## kumiawase

`kumiawase` 0.1.0 · needs: kazu

kumiawase (組み合わせ) — counting without enumerating.

| definition | what it does |
|---|---|
| `factorial(n)` |  |
| `permutations(n, k)` | Falling factorial: n * (n-1) * ... * (n-k+1) — permutations of k from n. |
| `combinations(n, k)` | Binomial coefficient, computed by the multiplicative formula. |
| `binomial(n, k)` |  |
| `pascal_row(n)` | The nth row of Pascal's triangle. |
| `catalan(n)` | Catalan number — the count that shows up in balanced parentheses, binary |
| `double_factorial(n)` | Double factorial: n * (n-2) * (n-4) * ... down to 2 or 1. |
| `rising_factorial(n, k)` | Rising factorial (Pochhammer): n * (n+1) * ... * (n+k-1). |
| `central_binomial(n)` | C(2n, n) — the middle and largest entry of an even Pascal row, and the term |
| `lattice_paths(w, h)` | Monotonic paths across a w-by-h grid, moving only right or up. |
| `multinomial(ks)` | Multinomial coefficient: how many distinct orderings a multiset with element |
| `combinations_with_repetition(n, k)` | Choose k from n with repetition allowed and order ignored — "n multichoose k", |
| `multichoose(n, k)` |  |
| `permutations_with_repetition(n, k)` | Ordered selections of k from n WITH repetition: n^k. |
| `binomial_row_sum(n)` | The sum of Pascal's nth row, which is also the number of subsets of an n-set. |
| `triangular(n)` | 1 + 2 + ... + n, equivalently C(n+1, 2) — the handshake count. |
| `pentagonal(n)` | n * (3n - 1) / 2: 1, 5, 12, 22, 35. The numbers Euler's partition recurrence |
| `hexagonal(n)` | n * (2n - 1): 1, 6, 15, 28. Every hexagonal number is a triangular one — |
| `tetrahedral(n)` | C(n+2, 3) — the triangular numbers stacked, i.e. cannonballs in a pyramid. |
| `fibonacci(n)` |  |
| `fib_onward(n, a, b)` | `a` is F(i) and `b` is F(i+1); each step slides the window one place right. |
| `lucas(n)` | Lucas numbers: the same recurrence as fibonacci, seeded 2, 1 instead of 0, 1. |
| `lucas_onward(n, a, b)` |  |
| `derangements(n)` | Derangements (subfactorial !n): permutations that leave NOTHING in place. |
| `derangements_onward(n, k, prev, cur)` | `prev` is D(k-1) and `cur` is D(k), walked up to k = n. |
| `subfactorial(n)` |  |
| `stirling2(n, k)` | Stirling numbers of the second kind: ways to split an n-set into exactly k |
| `stirling2_terms(n, k, j)` |  |
| `bell(n)` | Bell number: partitions of an n-set into ANY number of blocks, which is the |
| `surjections(n, k)` | Surjections: functions from an n-set ONTO all k elements of a k-set. |
| `partitions_with_max(n, m)` | Partitions of n using no part larger than m. |
| `partitions(n)` | p(n): 1, 1, 2, 3, 5, 7, 11 — the number of ways to write n as a sum of |
| `partitions_into(n, k)` | Partitions of n into EXACTLY k parts. |
| `compositions(n, k)` | Compositions: n written as an ORDERED sum of exactly k positive parts. |
| `compositions_total(n)` | Compositions of n into any number of parts: 2^(n-1), one binary choice at |
| `simplify_ratio(n, d)` | Reduce [numerator, denominator] to lowest terms, sign carried on the |
| `simplify_by_gcd(n, d, g)` | Divide both terms by g, flipping both signs when the denominator is the one |
| `coprime(a, b)` | Whether two counts share no factor. gcd(0, 0) is 0, so nothing is coprime to |
| `pascal_triangle(n)` | Rows 0 through n of Pascal's triangle. |

## moji

`moji` 0.1.0 · needs: retsu

moji (文字) — strings.

| definition | what it does |
|---|---|
| `empty(s)` |  |
| `present(s)` |  |
| `longer(a, b)` | Longer of two strings, ties going to the first. Ties must resolve |
| `char_at(s, i)` | The character at index i, as a one-character string; "" past either end. |
| `take_chars(s, n)` | The first n characters. Asking for more than there are yields all of them, |
| `drop_chars(s, n)` |  |
| `slice_chars(s, start, len)` | `len` characters starting at `start` — the LENGTH-based form. |
| `substring(s, start, stop)` | Characters from `start` up to but NOT including `stop` — the ENDPOINT-based |
| `starts_with(s, prefix)` | The runtime HAS `starts_with?`, `ends_with?` and `contains?` — and blue's |
| `ends_with(s, suffix)` |  |
| `char_index_from(s, sub, from)` | First index of `sub` at or after `from`, or -1. |
| `char_index(s, sub)` |  |
| `last_char_index_from(s, sub, from)` |  |
| `last_char_index(s, sub)` |  |
| `includes(s, sub)` |  |
| `occurrences(s, sub)` | How many NON-OVERLAPPING copies of `sub` are in `s`. |
| `before_first(s, sep)` | The two halves either side of the FIRST separator. |
| `after_first(s, sep)` |  |
| `before_last(s, sep)` | The same split, anchored at the LAST separator — which is the one a filename |
| `after_last(s, sep)` |  |
| `common_prefix_from(a, b, i)` |  |
| `common_prefix(a, b)` | The longest string both arguments start with. |
| `chunk_chars(s, n)` | Cut into fixed-width pieces, the last one short. |
| `repeated(s, n)` | `s` written n times end to end; "" for n < 1. |
| `fill_to(n, pad)` | Exactly n characters of `pad`, repeated and then cut. `pad` may be more than |
| `grow_to(acc, pad, n)` |  |
| `pad_left(s, width, pad)` | Pad to `width` columns. A string already at or past `width` is returned |
| `pad_right(s, width, pad)` |  |
| `pad_center(s, width, pad)` | Centre, with the ODD character going to the RIGHT. Like `longer`'s tie rule, |
| `replace_first(s, from, to)` | Replace only the FIRST occurrence — the half `replace` does not cover, since |
| `strip_prefix(s, p)` | Remove an affix IF it is there, and otherwise leave the string alone. |
| `strip_suffix(s, p)` |  |
| `ellipsize(s, width)` | Shorten to at most `width` columns, marking the cut with "…" spelled as three |
| `is_space(s)` | True when `s` is non-empty and every character in it is whitespace. |
| `is_blank(s)` | Nothing there to read: empty, or whitespace only. The predicate a caller |
| `words(s)` | Split on RUNS of whitespace, dropping the empties. |
| `split_words(cs, cur, acc)` |  |
| `lines(s)` | One entry per line, with "\r\n" normalised and NO phantom last line. |
| `squish(s)` | Trim, and collapse every internal whitespace run to a single space. |
| `wrap_text(s, width)` | Greedy word wrap: the lines are as full as they can be without exceeding |
| `wrap_from(ws, cur, acc, width)` |  |
| `capitalize(s)` | First character up, the REST DOWN — Ruby's `capitalize`, not "upcase the |
| `title_case(s)` | Every word capitalised, joined by single spaces. |
| `swap_char(c)` |  |
| `swap_case(s)` | Case inverted character by character. Caseless characters — digits, |
| `made_of(s, set)` | True when `s` is non-empty and every character of it appears in `set`. |
| `ascii_digits()` |  |
| `ascii_letters()` |  |
| `is_digit(s)` | ASCII, and said so rather than discovered. |
| `is_alpha(s)` |  |
| `is_alnum(s)` |  |
| `is_hex(s)` |  |
| `is_upper(s)` | Cased, and entirely in one case. |
| `is_lower(s)` |  |
| `same_text(a, b)` | Case-insensitive equality, for the comparisons a human means by "the same". |
| `sorts_before(a, b)` | Strict lexicographic order. |
| `insert_text(x, xs)` |  |
| `sort_strings(xs)` | Sort a list of strings, stably. |

## mokuroku

`mokuroku` 0.1.0 · needs: retsu, junjo, shuugou

mokuroku (目録): the catalogue of what every bidama on a BLUE_PATH provides.

| definition | what it does |
|---|---|
| `quoted_after(text, marker)` | The first quoted field after `marker`, and the one after it, as a list. |
| `parse_manifest(text)` | [name, version, needs] from a Bluefile's text. |
| `comment_text(line)` |  |
| `defined_name(line)` | The name a `def` line defines: everything up to "(" or a space. |
| `entries_of_text(text)` | [def_name, signature, doc_lines] for every top-level def in a source text. |
| `gloss_of_text(text)` | The package's gloss: the first comment line of its source. |
| `tests_in_text(text)` |  |
| `blue_path_roots(blue_path)` |  |
| `package_dirs(root)` | Package directories under a root: any directory holding a Bluefile. |
| `package_record(dir)` |  |
| `catalog_of(roots)` | Every package under the roots, sorted by name. When two roots hold the same |
| `pkg_name(r)` |  |
| `pkg_version(r)` |  |
| `pkg_needs(r)` |  |
| `pkg_gloss(r)` |  |
| `pkg_entries(r)` |  |
| `pkg_tests(r)` |  |
| `catalog_def_count(records)` |  |
| `definition_owners(records)` | [def_name, owner] for every definition, once per package. |
| `name_collisions(records)` | [def_name, [packages…]] for every name more than one package defines. |
| `name_collisions_touching(records, owned)` | The collisions that involve at least one of `owned` (a private distribution |
| `names_shadowing(records, names)` | [def_name, package] for every definition that reuses one of `names`. |
| `md_cell(s)` | Markdown table cells cannot hold a raw pipe. |
| `first_line_or_blank(lines)` |  |
| `pkg_needs_text(r)` |  |
| `pkg_summary_row(r)` |  |
| `def_row(e)` |  |
| `package_section(r)` |  |
| `render_markdown(records)` |  |
| `write_catalog(path)` | Write the catalogue of every root on BLUE_PATH to `path`. |

## nisshi

`nisshi` 0.1.0 · needs: retsu, shuugou, junjo, deeta, shomei, raifusaikuru

nisshi (日誌) — an event log: append-only JSON Lines, hash-chained, optionally signed, replayed into lifecycle states.

| definition | what it does |
|---|---|
| `el_format()` | The format tag, bound into every stream's genesis. |
| `el_genesis(label)` | The first hash of the stream named `label`. Two streams with different |
| `el_refusal_kinds()` | Every refusal kind a record can carry: lc_admit's. Closed. |
| `el_break_kinds()` | Every break el_verify reports, in the order it prefers them when two fall on |
| `el_key_text(k)` | A key as JSON text: a string itself, a keyword's or symbol's name. |
| `el_pair?(p)` | True for a [name, value] pair. |
| `el_object?(v)` | True for a value JSON writes as an object: a non-empty list whose every |
| `el_empty_object?(v)` | True for the value json_parse gives an empty object: a map that writes "{}". |
| `el_canon(v)` | The canonical JSON text of a value. Raises :eventlog_value for what JSON |
| `el_canon_list(v)` |  |
| `el_canon_float(x)` |  |
| `el_sorted_pairs(pairs)` | Pairs ordered by key text, in code-point order (what `compare` gives). |
| `el_require_pair(p)` |  |
| `el_canon_object(pairs)` | The canonical text of an object given as [name, value] pairs; "{}" for none. |
| `el_canon_member(p)` |  |
| `el_names(d)` | The definition's names indexed by their text: a map from an event type's |
| `el_key_index(keys)` |  |
| `el_require_distinct(texts, what)` |  |
| `el_type_named(names, t)` | The type named by text `t`: the definition's own name, or the text itself. |
| `el_key_named(names, ty, k)` | The payload key named by text `k` for type `ty`: the key the definition |
| `el_normal_event(names, ev)` | The event as the log holds it: its type and payload keys as the definition |
| `el_normal_value(v)` | A value with every object in it put in key order, which is all canonical |
| `el_refusal_of(verdict)` | The refusal an admission verdict gives a record: nil when admitted, else |
| `el_detail_texts(kind, detail)` |  |
| `el_refusal_kind(rf)` | A refusal's kind, one of el_refusal_kinds(). |
| `el_refusal_detail(rf)` | A refusal's detail, as text. |
| `el_refusal_text(rf)` |  |
| `el_normal_links(links)` | Links as the log holds them: [kind, id] pairs, the kind as text, in order. |
| `el_normal_link(l)` |  |
| `el_links_text(links)` |  |
| `el_link_text(l)` |  |
| `el_texts(entity, links, payload, refusal, seq, status, at, ty)` | The canonical text of each body field, in key order: entity, links, |
| `el_body_text(t)` | The text the chain hashes: the record without prev, hash and sig. |
| `el_line_text(t, prev, hash, sig)` | The line a stream holds: every field, keys in code-point order. |
| `el_sig_text(sig)` |  |
| `el_status_for(refusal)` |  |
| `el_record(position, seq, at, entity, ty, payload, links, status, refusal, prev, hash, sig, line)` | A record as read: [:el_record, position, seq, time, entity, type, payload, |
| `el_record?(x)` |  |
| `el_position(x)` | The index of a record or a bad line in its stream, from 0. |
| `el_rec_seq(r)` |  |
| `el_rec_time(r)` |  |
| `el_rec_entity(r)` |  |
| `el_rec_type(r)` | The type: the definition's own name when it declares it, else text. |
| `el_rec_payload(r)` | The payload as [key, value] pairs in key order. |
| `el_rec_links(r)` | The links as [kind, id] pairs, in the order they were given. |
| `el_rec_status(r)` | :admitted or :refused. |
| `el_rec_admitted?(r)` |  |
| `el_rec_refusal(r)` | nil, or [kind, detail] for a refused record. |
| `el_rec_prev(r)` |  |
| `el_rec_hash(r)` |  |
| `el_rec_sig(r)` | The signature's hex, or nil. |
| `el_rec_line(r)` | The line exactly as the stream holds it. |
| `el_rec_event(r)` | The event a record holds, as raifusaikuru folds it. |
| `el_texts_of(r)` |  |
| `el_body_of(r)` | The body text a record's hash covers, rendered from the record. |
| `el_line_of(r)` | The canonical line for a record: what its line must be, byte for byte. |
| `el_bad(position, line, why)` | A line that is not a record: [:el_bad_line, position, line, why]. |
| `el_bad_why(b)` |  |
| `el_decode_line(d, position, line)` | A line read back as a record, or as a bad line saying why it is not one. |
| `el_decode_named(names, position, line)` | el_decode_line with the definition's names already indexed (el_names), as |
| `el_decode_doc(names, position, line, doc)` | Each field is read once, with deeta's total readers (nil for absent or of |
| `el_payload_pairs(p)` |  |
| `el_status_of(t)` |  |
| `el_refusal_from(v)` |  |
| `el_shape_problem(entity, seq, at, ty_text, status, prev, hash, sig, payload, links, refusal)` | The first way a line's fields fail to make a record, as text, or nil. |
| `el_nonempty_string?(v)` |  |
| `el_natural?(v)` |  |
| `el_sig_shape?(v)` |  |
| `el_links_shape?(v)` |  |
| `el_link_shape?(l)` |  |
| `el_refusal_shape?(status, v)` |  |
| `el_texts_list?(v)` |  |
| `el_new(path, label, d, bytes, records)` | [:el_log, path, label, def, head, count, bytes, entities, index, last, |
| `el_require_log(log)` |  |
| `el_path(log)` |  |
| `el_label(log)` | The label the stream's genesis is made from. |
| `el_def(log)` |  |
| `el_head(log)` | The hash of the last record: what the next record's prev will be. |
| `el_count(log)` | How many lines the stream holds. |
| `el_bytes(log)` | The stream's size in bytes when this value was read or last appended. |
| `el_entities(log)` | The entities in the order they first appear. |
| `el_index(log)` |  |
| `el_last(log)` | The last record: after el_append, the record it wrote. |
| `el_records(log)` | The records as read, in stream order, bad lines included. Raises once the |
| `el_signer(log)` |  |
| `el_fault(log)` | The position of the first line that is not a record, or nil. |
| `el_names_of(log)` |  |
| `el_entry(log, entity)` | An entity's [state, next seq]; an unseen entity is at its lifecycle's start. |
| `el_state(log, entity)` | An entity's lifecycle state: its admitted records, folded. |
| `el_seq_of(log, entity)` | The seq an entity's next record will carry. |
| `el_states(log)` | Every entity's state, [entity, state], in the order entities first appear. |
| `el_signing(log, secret_hex)` | The log with a signing key: every record appended through it carries an |
| `el_absorb(log, x)` | Fold one line into the log, as read does. An admitted record steps its |
| `el_absorb_record(log, r, state)` | Advance the log past record `r`, its entity now in `state`. read passes |
| `el_next_seq(have)` |  |
| `el_absorb_bad(log, b)` |  |
| `el_entities_with(entities, e, known)` |  |
| `el_fold_record(d, state, r)` |  |
| `el_read(path, label, d)` | Read the stream at `path` (a missing file is an empty stream) and replay |
| `el_from_text(text, path, bytes, label, d)` | The log a stream's text reads as: el_read's work after the file is read. |
| `el_decode_at(names, i, line, is_torn)` |  |
| `el_lines(text)` | A stream's lines: its text split on newlines, without the empty piece after |
| `el_unlines(lines)` | Lines back into a stream's text, each ended by a newline. |
| `el_open(path, label, d, public_hex)` | The writer's door: read the stream, verify it (signatures too, given a |
| `el_append(log, entity, event, links)` | Append `event` (an lc_ev) for `entity`, with `links` ([kind, id] pairs, [] |
| `el_state_after(state, judged, refusal)` | The entity's state after an append: the step's when admitted, unchanged |
| `el_sign(secret, hash)` |  |
| `el_bytes_on_disk(path)` |  |
| `el_require_writable(log)` |  |
| `el_require_entity(entity)` |  |
| `el_require_event(ev)` |  |
| `el_require_round_trip(r, line, ev)` | Refuse to write a line that does not read back as the record it renders |
| `el_verify(log, public_hex, expected_head)` | Walk the stream and report the first break, or that it is intact: |
| `el_before_bad(records, bad)` |  |
| `el_head_of(g, good)` |  |
| `el_earlier(best, b)` |  |
| `el_break_bad(bad)` |  |
| `el_break_chain(g, good, texts)` | The first record the chain does not reproduce (shomei's chain_first_break), |
| `el_break_canon(good, texts)` |  |
| `el_canonical_at?(r, t)` |  |
| `el_break_seq(good)` |  |
| `el_seq_step(acc, r)` |  |
| `el_break_sig(good, public_hex)` |  |
| `el_sig_break(r)` |  |
| `el_sig_ok?(r, public_hex)` |  |
| `el_intact?(rep)` | True for a report of an intact stream. |
| `el_break_position(rep)` | The position of a broken report's first break, from 0. |
| `el_break_kind(rep)` |  |
| `el_break_why(rep)` |  |
| `el_by_entity(records)` | The records grouped by entity: [[entity, records]] in the order entities |
| `el_group_add(a, r)` |  |
| `el_example_events(n, k)` | `n` events over `k` of raifusaikuru's example consumables, interleaved: |
| `el_example_event(i, k)` |  |
| `el_example_step(step, at)` |  |
| `el_example_links(i)` |  |
| `el_append_all(log, items)` | Append every [entity, event, links] in order. |
| `el_test_path(name)` | A fresh path under TMPDIR for a test's stream. |
| `el_identity_step(acc, x)` |  |
| `el_doc_stream(p)` | The stream the b3sum test pins: three events for one item, signed with RFC |
| `el_control_lines(p, secret)` | A signed stream of five records over two items, as its lines: the base the |
| `el_control_rows(lines, head, k, other)` | One damaged copy per break kind: [kind, text, public key, expected head, |
| `el_wrong_seq_lines(lines, secret)` | The stream with its last record rewritten to claim seq 1 for item-2's first |
| `el_control_verdict(row)` |  |

## ongaku

`ongaku` 0.1.0 · needs: retsu, kazu

ongaku (音楽) — twelve-tone music theory, as arithmetic on semitones.

| definition | what it does |
|---|---|
| `note_names()` |  |
| `note_name(pc)` |  |
| `transpose_note(pc, semitones)` |  |
| `scale_from(root, steps)` | Build a scale by walking a list of intervals from a root. |
| `major_scale(root)` |  |
| `minor_scale(root)` |  |
| `major_triad(root)` |  |
| `minor_triad(root)` |  |
| `dominant_seventh(root)` |  |
| `interval_name(semitones)` |  |
| `midi_to_hz(n)` | MIDI note number to frequency in Hz. 69 is A4 = 440. |
| `pitch_class(n)` | The normaliser every function below shares. `%` is Euclidean on this runtime |
| `wrap_index(i, n)` | The same fold for a list of length n rather than for the twelve pitches: |
| `note_names_flat()` | The same twelve pitches, spelled with flats. |
| `note_name_flat(pc)` |  |
| `note_name_in(pc, use_flats)` |  |
| `pitch_class_of(name)` | Name -> pitch class. Returns 0 - 1 for a name it cannot place, matching |
| `crossing_names()` | The four spellings that cross a letter boundary. E#, Fb, B# and Cb are in |
| `pitch_class_of_crossing(name)` |  |
| `enharmonic_of(name)` | The other spelling of the same pitch. A natural has only one spelling in |
| `is_enharmonic(a, b)` |  |
| `diminished_triad(root)` | The two triads the existing pair leaves out. Together the four exhaust the |
| `augmented_triad(root)` |  |
| `sus2_triad(root)` | Suspensions are not a third quality — they have no third at all, which is the |
| `sus4_triad(root)` |  |
| `major_seventh(root)` |  |
| `minor_seventh(root)` |  |
| `half_diminished_seventh(root)` | Minor triad, minor seventh, flat fifth — the ii of a minor key. Called |
| `diminished_seventh(root)` | Four minor thirds stacked, so it divides the octave evenly: every inversion |
| `minor_major_seventh(root)` | The tonic seventh of harmonic minor — minor triad under a MAJOR seventh, |
| `augmented_major_seventh(root)` |  |
| `augmented_seventh(root)` |  |
| `chord_intervals(notes)` | A chord reduced to its shape: the intervals of every note above the lowest. |
| `chord_from_root(root, intervals)` |  |
| `quality_of_shape(shape, shapes, names)` | Shape -> name, with "unknown" for anything unlisted. An unlisted shape is a |
| `triad_shapes()` |  |
| `triad_quality_names()` |  |
| `triad_quality(notes)` |  |
| `seventh_shapes()` |  |
| `seventh_quality_names()` |  |
| `seventh_quality(notes)` |  |
| `chord_quality(notes)` | Dispatches on how many notes there are, since a three-note shape and a |
| `chord_name(notes)` |  |
| `invert_chord(notes, n)` | Inversion in pitch-class space is rotation: the notes are the same, the |
| `major_steps()` | The seven modes are ROTATIONS of one interval pattern, not seven patterns. |
| `mode_names()` |  |
| `mode_steps(name)` |  |
| `mode_scale(root, name)` | An unrecognised mode name gives [] rather than a one-note scale, because |
| `mode_of_major(root, degree)` | The other, equivalent definition: the mode you land on by starting a major |
| `harmonic_minor_scale(root)` | Harmonic minor raises the seventh so the chord on V is major and can pull |
| `melodic_minor_scale(root)` | Melodic minor, ascending form — sixth AND seventh raised, which is what |
| `major_pentatonic_scale(root)` |  |
| `minor_pentatonic_scale(root)` |  |
| `blues_scale(root)` | The minor pentatonic with the flat fifth wedged in — one passing note, and |
| `chromatic_scale(root)` |  |
| `whole_tone_scale(root)` | Six equal whole steps. It divides the octave evenly, so it is one of the two |
| `scale_tones(scale)` | Every scale here ends on the octave restatement of its root, so `size` counts |
| `is_in_scale(pc, scale)` |  |
| `scale_degree_of(pc, scale)` | Zero-based degree — 0 is the tonic, so degree 4 is the dominant. 0 - 1 when |
| `transpose_all(notes, semitones)` | Transposes any note list — a scale, a chord, a melody. Kept separate from |
| `note_names_of(notes)` |  |
| `scale_note(scale, degree)` | The degree-th note of a scale, wrapping. Degrees are zero-based here to match |
| `scale_triad(scale, degree)` | A chord on a scale degree is thirds stacked WITHIN the scale — skip a note, |
| `scale_seventh(scale, degree)` |  |
| `scale_triad_qualities(scale)` | The quality of every degree's triad, in order — the "I ii iii IV V vi vii°" |
| `scale_seventh_qualities(scale)` |  |
| `fifth_up(pc)` |  |
| `fifth_down(pc)` |  |
| `circle_of_fifths(start)` | Stepping by fifths visits all twelve pitches before repeating, because 7 and |
| `key_signature(pc)` | Accidentals in a MAJOR key: positive counts sharps, negative counts flats. |
| `sharp_count(pc)` |  |
| `flat_count(pc)` |  |
| `key_uses_flats(pc)` |  |
| `note_name_for_key(pc, key)` | Spelling driven by the key rather than by preference — the reason both name |
| `key_note_names(key)` | The scale of a major key, spelled the way that key is written. |
| `sharps_in_order()` | Sharps are added F C G D A E B and flats in the exact reverse. It is the same |
| `flats_in_order()` |  |
| `key_accidentals(pc)` | Which letters carry an accidental in this key, in the order a copyist writes |
| `relative_minor(pc)` | A major key and its relative minor share every note and every accidental; |
| `relative_major(pc)` |  |
| `minor_key_signature(pc)` | A minor key's signature is its relative major's, by definition rather than by |
| `interval_between(a, b)` | Semitones from a UP to b. Directed, so it is deliberately not symmetric: |
| `interval_name_between(a, b)` |  |
| `interval_class(a, b)` | The undirected distance, 0..6. A fifth up and a fourth down separate the same |
| `invert_interval(semitones)` |  |
| `is_consonant(semitones)` | Consonance in the common-practice sense: unison, both thirds, both sixths, |
| `is_dissonant(semitones)` |  |
| `note_to_midi(pc, octave)` | Octave numbering follows scientific pitch notation, the convention the |
| `midi_octave(n)` | floor_div rather than a bare division, so notes below C0 keep counting down |
| `midi_pitch_class(n)` | The MIDI-facing name for the same fold, so the pair that decomposes a note |
| `midi_note_name(n)` |  |
| `note_to_hz(pc, octave)` |  |
| `hz_to_midi(hz)` | The inverse of midi_to_hz. Returns a FRACTIONAL note number on purpose — the |
| `nearest_midi(hz)` |  |
| `cents_off(hz)` | How far off the nearest tempered note a frequency is, in cents. Signed: |
| `cents_of_ratio(ratio)` | Cents are a logarithmic unit — 1200 to the octave — which is what makes |
| `cents_between(hz1, hz2)` |  |
| `equal_ratio(semitones)` | Equal temperament: the octave cut into twelve identical ratios. Every |
| `just_ratio_table()` | Five-limit just intonation: intervals as small whole-number ratios, which is |
| `just_ratio_parts(semitones)` | Ratio as exact numerator and denominator, octave-extended: an interval wider |
| `reduce_ratio(num, den)` |  |
| `just_ratio(semitones)` |  |
| `just_hz(base_hz, semitones)` |  |
| `temperament_error_cents(semitones)` | How far equal temperament sits from the pure interval, in cents. Signed, and |

## raifusaikuru

`raifusaikuru` 0.1.0 · needs: retsu, shuugou

raifusaikuru (ライフサイクル) — lifecycles: event-sourced states, guards that name the rule and the task, and permit limits.

| definition | what it does |
|---|---|
| `lc_text(x)` | A name as text: a keyword's name, a string itself, anything else via to_s. |
| `lc_show(x)` | A value as it reads in a message: keywords keep their colon, strings their |
| `lc_name?(x)` |  |
| `lc_minutes(n)` | Durations on a seconds clock. |
| `lc_hours(n)` |  |
| `lc_days(n)` |  |
| `lc_states(names)` | Declare states. May appear more than once; the mentions accumulate. |
| `lc_start(state)` | The state a new entity starts in: exactly one per definition. |
| `lc_terminals(names)` | Terminal states: an entity rests there for good, and no event leaves them. |
| `lc_rests(names)` | Resting states that are not sinks (an asset in service): an entity may stay |
| `lc_field(name, initial)` | A field the events fold into, with its value before any event. |
| `lc_event(name, keys)` | An event type and the payload keys it must carry. A declared key is |
| `lc_on(from, event, to, effects)` | One transition row: in `from`, event `event` moves the entity to `to` and |
| `lc_stay_to(from, to)` |  |
| `lc_on_each(froms, event, to, effects)` | The same row from each of several states. With :stay each row loops on its |
| `lc_gate(capability, rows)` | Gate rows by a capability: the event is a USE of it. The fold evaluates the |
| `lc_gated_row(capability, c)` |  |
| `lc_set(field, key)` | Set a field from a payload key of the event. |
| `lc_put(field, value)` | Set a field to a constant. |
| `lc_add(field, n)` | Add a constant to a numeric field: a counter. |
| `lc_add_from(field, key)` | Add a numeric payload value to a numeric field. |
| `lc_stamp(field)` | Set a field to the event's time. |
| `lc_below(field, limit)` | The field is a number below the limit. |
| `lc_at_most(field, limit)` |  |
| `lc_above(field, limit)` |  |
| `lc_at_least(field, limit)` |  |
| `lc_equals(field, value)` | The field equals the value. A float never equals an int: 1.0 is not 1. |
| `lc_present(field)` | The field has a value (is not nil). Never unknown. |
| `lc_fresh(field, max_age)` | The field holds a time less than `max_age` before now: a reading still |
| `lc_in_phase(states)` | The entity is in one of these states. |
| `lc_all(preds)` | Every predicate holds. Unknown when none refuses and one is unknown. |
| `lc_any(preds)` | Some predicate holds. Unknown when none holds and one is unknown. |
| `lc_not(pred)` | The predicate does not hold. The negation of unknown is unknown. |
| `lc_task(name, evidence, escalate_after)` | A task spec: its name, the evidence kinds that close it, and how long it may |
| `lc_task_name(t)` |  |
| `lc_task_evidence(t)` |  |
| `lc_task_escalate_after(t)` |  |
| `lc_rule(name, pred)` | A named rule. A guard allows its capability only when every rule holds. |
| `lc_rule_task(name, pred, task)` | A named rule whose refusal asks for its own task. |
| `lc_guard(capability, rules, task)` | The guard for a capability: its rules, all of which must hold, and the task |
| `lc_valid_for(duration)` | A permit lives at most `duration` from its issue: the renewal cadence. |
| `lc_counted(field)` | The device counts one unit of `field` per use, so the permit carries how many |
| `lc_permit(capability, terms)` | A capability enforced away from the engine, by a device, gets a permit. Its |
| `lc_flat(clauses)` | A clause is a list headed by a keyword; anything else is a list of clauses |
| `lc_flat_one(c)` |  |
| `lc_tagged(flat, tag)` |  |
| `lc_known_tags()` |  |
| `lc_parse(name, clauses)` | The parsed form, which lc_define seals as the definition once it validates: |
| `lc_d_name(d)` |  |
| `lc_d_flat(d)` |  |
| `lc_d_states(d)` |  |
| `lc_d_starts(d)` |  |
| `lc_d_start(d)` |  |
| `lc_d_terminals(d)` |  |
| `lc_d_rests(d)` |  |
| `lc_d_fields(d)` |  |
| `lc_d_field_names(d)` |  |
| `lc_d_events(d)` |  |
| `lc_d_event_names(d)` |  |
| `lc_d_edges(d)` |  |
| `lc_d_guards(d)` |  |
| `lc_d_capabilities(d)` |  |
| `lc_d_permits(d)` |  |
| `lc_problem_kinds()` | Every kind of problem the builder reports. Closed: lc_problem refuses any |
| `lc_problem(kind, why)` |  |
| `lc_problem_kind(p)` | A problem's kind, one of lc_problem_kinds(). |
| `lc_problem_why(p)` | A problem's explanation, naming what is wrong and where. |
| `lc_problem_text(p)` |  |
| `lc_dupes(xs)` | Every value that occurs more than once, once each, in first-seen order. |
| `lc_positive_int?(x)` |  |
| `lc_problems(name, clauses)` | Why a definition would be refused: a list of [kind, why], empty when it is |
| `lc_check(p)` |  |
| `lc_pb_shape(p)` |  |
| `lc_pb_clause(c)` |  |
| `lc_pb_names(p)` |  |
| `lc_pb_state(s, states, where)` |  |
| `lc_pb_start(p)` |  |
| `lc_edge_where(e)` |  |
| `lc_pb_edges(p)` |  |
| `lc_pb_edge(p, e)` |  |
| `lc_pb_field(f, fields, where)` |  |
| `lc_pb_effect(p, eff, keys, where)` |  |
| `lc_forward(pairs, seeds, n)` | States reachable from `seeds` along `pairs` ([from, to]), within n rounds. |
| `lc_backward(pairs, goals, n)` | States that can reach `goals` along `pairs`, within n rounds. |
| `lc_pb_graph(p)` |  |
| `lc_pb_graph_of(p, states)` |  |
| `lc_pb_pred(pr, fields, states, where)` |  |
| `lc_pb_task(t, where)` |  |
| `lc_rule?(r)` |  |
| `lc_pb_guards(p)` |  |
| `lc_pb_guard(p, g)` |  |
| `lc_conjuncts(pr)` | The atoms in positive conjunctive position: each rule's predicate, and |
| `lc_atoms(pr)` |  |
| `lc_hidden_atoms(pr)` | Every atom beneath an lc_any or an lc_not. |
| `lc_guard_pred(g)` | The guard as one predicate: the conjunction of its rules. |
| `lc_drifts?(atom, counted)` | An atom that changes as a permit is used: any lc_fresh (time passes), or a |
| `lc_pb_permits(p)` |  |
| `lc_pb_permit(p, pm)` |  |
| `lc_pb_permit_terms(p, pm, g, where)` |  |
| `lc_permit_term?(t, fields)` |  |
| `lc_define(name, clauses)` | Build a lifecycle definition from clauses, or throw :raifusaikuru_definition |
| `lc_require(d)` |  |
| `lc_name(d)` | The definition's name. |
| `lc_edge_for(d, phase, event)` |  |
| `lc_legal_events(d, phase)` | The events accepted in a state, in declaration order: what an illegal |
| `lc_guard_for(d, capability)` |  |
| `lc_permit_spec_for(d, capability)` |  |
| `lc_ev(type, time, payload)` | An event: its type, its time on the caller's clock, and its payload as |
| `lc_ev_type(ev)` |  |
| `lc_ev_time(ev)` |  |
| `lc_ev_payload(ev)` |  |
| `lc_ev_get(ev, key)` | A payload value, or nil when the key is absent. |
| `lc_ev_has?(ev, key)` |  |
| `lc_initial(d)` | The state before any event. A state is |
| `lc_phase(s)` | The state the entity is in. |
| `lc_fields(s)` | Every field as [name, value], in declaration order. |
| `lc_value(s, field)` | A field's value, or nil. |
| `lc_seq(s)` | How many events the state has consumed, accepted or refused: the position |
| `lc_as_of(s)` | The time of the last applied event, or nil before any. |
| `lc_refused(s)` | Refused events, oldest first: [position, type, kind, detail]. kind is |
| `lc_refusal_position(r)` |  |
| `lc_refusal_type(r)` |  |
| `lc_refusal_kind(r)` |  |
| `lc_refusal_detail(r)` |  |
| `lc_breaches(s)` | Breaches, oldest first: [position, type, capability, refusing rules]. A gated |
| `lc_breach_position(b)` |  |
| `lc_breach_type(b)` |  |
| `lc_breach_capability(b)` |  |
| `lc_breach_rules(b)` |  |
| `lc_judge(d, s, ev)` | The one decision about an event in a state: |
| `lc_judge_row(d, s, ev, e)` |  |
| `lc_put_field(fs, f, v)` |  |
| `lc_put_pair(kv, f, v)` |  |
| `lc_effect(fs, eff, ev)` |  |
| `lc_step(d, s, ev)` | One event, folded. Total: a refused event is recorded and changes nothing |
| `lc_apply(d, s, ev, j)` | The state after an event lc_judge has already decided: the second half of |
| `lc_resume(d, s, events)` | Continue a fold from a state. |
| `lc_state_of(d, events)` | Replay an entity's events, in order, into its state. |
| `lc_admit(d, s, ev)` | Would `ev` be accepted in `s`? [:admit], or [:reject, kind, detail] with the |
| `lc_admission(j)` | lc_judge's decision as lc_admit answers it. |
| `lc_admit_step(d, s, ev)` | lc_admit and lc_step from ONE judgement: [admission, state after]. An |
| `lc_admitted?(a)` |  |
| `lc_reject_kind(a)` |  |
| `lc_reject_detail(a)` |  |
| `lc_k(b)` |  |
| `lc_k_and(vs)` |  |
| `lc_k_or(vs)` |  |
| `lc_k_not(v)` |  |
| `lc_compare(op, v, limit)` |  |
| `lc_eval(pr, s, now)` | A predicate over a state at time `now`: :yes, :no or :unknown. A comparison |
| `lc_task_or(rule, default)` |  |
| `lc_allowed(d, s, capability, now)` | Is `capability` allowed in `s` at time `now`? The verdict is data: |
| `lc_is_allowed(v)` |  |
| `lc_verdict_capability(v)` |  |
| `lc_refused_rules(v)` | The refusing rules' names, in the guard's order; [] when allowed. |
| `lc_refused_rule(v)` | The rule that refused (the first, in the guard's order), or nil when allowed. |
| `lc_verdict_tasks(v)` | The task specs a refusal asks for; [] when allowed. |
| `lc_pred_fields(pr)` | The fields a predicate reads, once each, in first-seen order. |
| `lc_observed(pr, s)` |  |
| `lc_pred_has?(pr, target)` |  |
| `lc_pred_text(pr)` | A predicate as words: "quality below 24". |
| `lc_observed_text(kv)` |  |
| `lc_reason_text(r)` |  |
| `lc_why(v)` | Each refusal as a sentence a person can act on: |
| `lc_min_known(xs)` |  |
| `lc_time_bound(g, s)` | The moment the guard could first stop holding as time passes: the earliest |
| `lc_count_bound(g, s, field)` | How many more uses (one unit of `field` each) keep every conjunct on it |
| `lc_uses_left(op, v, limit)` |  |
| `lc_permit_for(d, s, capability, now)` | The permit for `capability` in `s` at `now`, as data: |
| `lc_permit_limits(d, s, spec, now)` |  |
| `lc_has_permit?(p)` |  |
| `lc_permit_lifecycle(p)` |  |
| `lc_permit_capability(p)` |  |
| `lc_permit_issued_at(p)` |  |
| `lc_permit_not_after(p)` |  |
| `lc_permit_counts(p)` |  |
| `lc_permit_count(p, field)` | The remaining count of one counted field. |
| `lc_permit_basis(p)` |  |
| `lc_no_permit_why(p)` | Why there is no permit (:no_permit_declared, :refused, :unbounded, |
| `lc_no_permit_detail(p)` |  |
| `lc_permit_payload(p, subject)` | THE SIGNING SEAM. The canonical text of a permit issued to `subject` (the |
| `lc_example_consumable()` | A consumable, generic: sealed until opened, then used until spent or |
| `lc_example_log()` | The example's log: a reading of 10 at 1000, opened at 2000, three uses. |
| `lc_example_base()` | One row per problem kind: [defect, kind]. Each defect is added to a sound |
| `lc_example_problem_rows()` |  |

## ran

`ran` 0.1.0 · needs: retsu, shuugou, junjo

ran (乱) — deterministic pseudo-randomness.

| definition | what it does |
|---|---|
| `next_seed(seed)` |  |
| `next_int(seed, bound)` |  |
| `next_float(seed)` |  |
| `next_range(seed, lo, hi)` |  |
| `take_ints(seed, bound, n)` | A list of n values, threading the seed through so the sequence advances. |
| `seeds(seed, n)` |  |
| `choice(seed, xs)` | Pick an element by index. Returns the element, not the seed — callers who |
| `next_uniform(seed, bound)` | A draw in [0, bound) taken from the TOP of the word rather than the bottom. |
| `next_bool(seed)` | The top bit, for the same reason: `next_int(seed, 2)` would be a metronome. |
| `take_bools(seed, n)` |  |
| `take_floats(seed, n)` | n floats in [0, 1). Never reaches 1: the largest seed is 2^32 - 1. |
| `roll(seed, sides)` | Dice are 1-based, which is the whole reason these exist rather than callers |
| `roll_dice(seed, count, sides)` |  |
| `roll_sum(seed, count, sides)` |  |
| `roll_best(seed, count, sides)` | Roll with advantage: the best of `count` dice, reproducibly. |
| `roll_worst(seed, count, sides)` |  |
| `split_seed(seed)` | Two seeds whose streams do not overlap, for handing an independent source to |
| `mix_seed(x)` |  |
| `stream_seed(seed, i)` | The seed of stream i from one experiment seed. Streams for different i are |
| `pick(seed, xs)` | `choice` off the top of the word. Kept separate rather than folded into |
| `choices(seed, xs, n)` | n picks WITH replacement — repeats are expected, and the list is not consumed. |
| `shuffle(seed, xs)` | Fisher-Yates, written as select-and-remove because there is no mutable array |
| `sample_n(seed, xs, n)` | n picks WITHOUT replacement. Asking for more than there is gives everything |
| `total_weight(ws)` |  |
| `weight_walk(xs, ws, r)` | Walk the weights subtracting as we go — the inverse-CDF pick. |
| `weighted_choice(seed, xs, ws)` | Weights need not be normalised and need not be integers; they are divided by |
| `next_gaussian(seed)` | Box-Muller. Consumes TWO words of the stream, which is why take_gaussians |
| `next_normal(seed, mean, sd)` |  |
| `take_gaussians(seed, n)` |  |
| `next_step(seed)` |  |
| `random_walk(seed, n)` | Positions, not steps: the caller wants the path. Each entry differs from the |

## retsu

`retsu` 0.1.0 · needs: kazu

retsu (列) — lists, made TOTAL.

| definition | what it does |
|---|---|
| `size(xs)` | Total length: nil counts as empty rather than erroring. |
| `is_empty(xs)` |  |
| `first(xs)` |  |
| `rest(xs)` | The tail, NORMALISED to a list. |
| `last(xs)` |  |
| `push(xs, v)` |  |
| `concat_lists(a, b)` |  |
| `flatten1(xss)` |  |
| `indexes(xs)` |  |
| `zip_with(f, a, b)` |  |
| `zip(a, b)` |  |
| `repeat(v, n)` |  |
| `sum_to(n)` |  |
| `count_down(n)` |  |
| `clamped_sum(n, lo, hi)` | The cross-package call: clamp comes from kazu. |
| `take_n(xs, n)` | take/drop, total and data-first. |
| `drop_n(xs, n)` |  |
| `slice(xs, start, count)` | A window by position and length. Out-of-range in either argument yields the |
| `all_but_last(xs)` |  |
| `index_of(xs, v)` | Position of the first `v`, or 0 - 1 when absent. |
| `index_of_from(xs, v, i)` |  |
| `contains(xs, v)` |  |
| `count_of(xs, v)` |  |
| `without(xs, v)` |  |
| `as_list(xs)` | nil-to-[] normalisation, in one place. |
| `insert_at(xs, i, v)` |  |
| `remove_at(xs, i)` |  |
| `update_at(xs, i, v)` | Replace, rather than remove-and-insert, so size is preserved and an |
| `partition_at(xs, i)` |  |
| `chunk(xs, n)` | Fixed-size groups, last one short. |
| `windows(xs, n)` | Overlapping windows of width n — chunk's sliding sibling. |
| `rotate(xs, n)` | Rotate left by n, wrapping. Negative n rotates right, and n past the end |
| `interleave(a, b)` |  |
| `find_first(f, xs)` | The first element satisfying f, or nil. |
| `reject(f, xs)` |  |
| `count_where(f, xs)` |  |
| `partition(f, xs)` | Both halves of one pass, in the order [kept, rejected]. |
| `take_while(f, xs)` | take_while stops at the FIRST failure and does not resume — the difference |
| `drop_while(f, xs)` |  |
| `scan(f, init, xs)` | Every intermediate accumulator of a `reduce`, init included, so the result is |
| `scan_from(f, acc, xs)` |  |
| `running_sum(xs)` | Running totals in the usual sense — same length as the input, no leading |
| `enumerate(xs)` |  |
| `flat_map(f, xs)` |  |
| `unzip(ps)` | The inverse of zip: a list of pairs back into [firsts, seconds]. |
| `equal_lists(a, b)` | Element-wise equality that treats the two empties as one. |
| `is_prefix_of(p, xs)` | Read as "p is a prefix of xs" — the subject comes first, as in the name. |
| `is_suffix_of(s, xs)` |  |
| `flatten_n(xss, depth)` | Flatten exactly `depth` levels. |

## ronri

`ronri` 0.1.0 · needs: retsu, kansuu

ronri (論理) — predicates, and the algebra over them.

| definition | what it does |
|---|---|
| `every(p, xs)` |  |
| `any(p, xs)` |  |
| `none(p, xs)` |  |
| `count_if(p, xs)` |  |
| `complement(p)` |  |
| `both(p, q)` |  |
| `either(p, q)` |  |
| `xor(p, q)` | The rest of the connectives, so a caller never has to spell one out by hand. |
| `implies(p, q)` | Material implication — FALSE antecedent makes it TRUE. That is the case |
| `nand(p, q)` |  |
| `nor(p, q)` |  |
| `all_of(ps)` | all_of / any_of / none_of take a LIST of predicates and hand back a |
| `any_of(ps)` |  |
| `none_of(ps)` |  |
| `is_empty_pred(xs)` | Predicates ABOUT lists, so `count_if(is_nonempty_pred, rows)` reads without a |
| `is_nonempty_pred(xs)` |  |
| `exactly_n(n, p, xs)` | The counting quantifiers, between `any` (n >= 1) and `every` (n == size). |
| `at_least_n(n, p, xs)` |  |
| `at_most_n(n, p, xs)` |  |
| `majority(p, xs)` | Strictly MORE than half — a tie is not a majority. |
| `partition_by(p, xs)` | partition_by(p, xs) => [satisfying, not_satisfying]. |
| `prefix_len(p, xs)` | How many elements at the FRONT satisfy p. This is the only recursion in the |
| `take_while_pred(p, xs)` |  |
| `drop_while_pred(p, xs)` |  |
| `take_until(p, xs)` | take_until stops BEFORE the first element that satisfies p, and stops for |
| `drop_until(p, xs)` |  |
| `span_by(p, xs)` | span_by(p, xs) => [prefix, remainder], the take_while_pred/drop_while_pred pair. |
| `equals(v)` | Lift a VALUE into a predicate — the constructor that lets the rest of this |
| `via(f, p)` | via(f, p) asks p about f(x) rather than about x — `count_if(via(abs, |
| `find_index_from(p, xs, i)` | The index of the first satisfying element, or 0 - 1 when there is none. |
| `find_index_by(p, xs)` |  |
| `find_by(p, xs)` | The first satisfying ELEMENT, or nil — nil here matches retsu's first([]), |
| `indexes_where(p, xs)` | Every index whose element satisfies p; find_index_by is its first entry. |

## ryouiki

`ryouiki` 0.1.0 · needs: retsu, junjo, toukei, ran

ryouiki (領域) — region: which region of the inputs produces an outcome.

| definition | what it does |
|---|---|
| `box_around(points)` | The box around every point: [min, max] of each input. |
| `in_box?(box, p)` |  |
| `inside_of(box, points)` |  |
| `mean_target(ids, ys)` |  |
| `peeled_edge(values, side, alpha)` | The narrower edge for one input and side: the alpha-quantile of the values |
| `peel_candidates(box, ids, points, ys, alpha)` | Every candidate peel from `box`: [box', inside', density']. |
| `prim(points, ys, alpha, min_support)` | The peeling trajectory. alpha: the share peeled per step (0.05 is PRIM's |
| `if_zero_total(total, value)` |  |
| `prim_box(trajectory, density)` | The box to report: the most coverage among steps at or above `density`, |
| `box_text(names, box, full)` | A box as text over named inputs: "x in [5, 9], y in [0, 3]" — only the |
| `ryouiki_grid(k)` |  |

## sabi

`sabi` 0.1.0 · needs: retsu

sabi (錆): Rust source from blue data. Build the items as values, render once.

| definition | what it does |
|---|---|
| `rs_ty(name)` |  |
| `rs_ty_ref(inner)` |  |
| `rs_ty_slice(inner)` |  |
| `rs_ty_tuple(items)` |  |
| `rs_render_ty(t)` |  |
| `rs_str(s)` |  |
| `rs_char(c)` |  |
| `rs_int(n)` |  |
| `rs_bool(b)` |  |
| `rs_slice(items)` | `&[a, b, …]`, one element per line. |
| `rs_tuple(items)` |  |
| `rs_path(segments)` | `A::B::C`. |
| `rs_match(scrutinee, arms)` |  |
| `rs_arm(pattern, expr)` |  |
| `rs_escape_str(s)` | A Rust string literal's body: backslash first, so the escapes added after it |
| `rs_escape_char(c)` |  |
| `rs_pad(level)` |  |
| `rs_render_expr(e, level)` | An expression at an indentation level (the level of the line it starts on). |
| `rs_const(name, ty, value, doc)` |  |
| `rs_variant(name, doc)` |  |
| `rs_enum(name, derives, variants, doc)` |  |
| `rs_field(name, ty, doc)` |  |
| `rs_struct(name, derives, fields, doc)` |  |
| `rs_param(name, ty)` |  |
| `rs_self_param()` | `&self`. |
| `rs_fn(name, params, ret, body, doc)` |  |
| `rs_impl(ty_name, fns)` |  |
| `rs_doc_line(line, level, marker)` |  |
| `rs_doc_lines(doc, level, marker)` |  |
| `rs_derive_line(derives, level)` |  |
| `rs_render_param(p)` |  |
| `rs_render_fn(f, level)` |  |
| `rs_render_item(item)` |  |
| `get_or_empty(m, key)` |  |
| `rs_keywords()` |  |
| `rs_ident_start_chars()` |  |
| `rs_ident_chars()` |  |
| `rs_valid_ident?(s)` | An ASCII Rust identifier that is not a keyword. |
| `rs_item_names(item)` | Every name an item declares. |
| `rs_invalid_names(file)` | The names in a file that are not valid Rust identifiers. Empty when clean. |
| `rs_file(source, items)` | `source` names the blue program that generated the file, for the header. |
| `render_rust(file)` |  |

## seimei

`seimei` 0.1.0 · needs: retsu

seimei (生命) — cellular automata: elementary rules, and Life.

| definition | what it does |
|---|---|
| `elementary_step_cell(rule, l, c, r)` | One cell's next state under an elementary rule, from its three-cell |
| `bit_of(n, i)` |  |
| `elementary_step(rule, row)` | One generation of an elementary automaton, with the row treated as a ring so |
| `elementary_run(rule, row, gens)` |  |
| `cell_at(grid, r, c)` | Conway's Life on a bounded grid, cells outside counting as dead. |
| `neighbours(grid, r, c)` |  |
| `life_cell(grid, r, c)` |  |
| `life_step(grid)` |  |
| `population(grid)` |  |
| `grid_rows(grid)` | Dimensions, total on the empty grid: `size(first(grid))` is the expression |
| `grid_cols(grid)` |  |
| `blank_grid(rows, cols)` |  |
| `life_from_coords(rows, cols, coords)` | A grid from a sparse [[row, col], ...] list — how patterns are actually |
| `live_coords(grid)` | The live coordinates of a grid — the inverse of life_from_coords, so the two |
| `translate(grid, dr, dc)` | Shift the contents by (dr, dc), keeping the frame the same size. Whatever |
| `glider(rows, cols)` | The canonical glider, top-left, in phase 0: |
| `after_with(f, grid, gens)` | Iteration, once, for every rule set in this package. A step function is the |
| `run_with(f, grid, gens)` |  |
| `life_after(grid, gens)` | The state after n generations. life_run keeps the history; this keeps only |
| `life_run(grid, gens)` |  |
| `is_stable(grid)` |  |
| `is_oscillator(grid, max_period)` | The period of the pattern, or 0 if it has not returned within max_period. |
| `period_with(f, grid, max_period)` |  |
| `period_scan(f, start, now, p, max_period)` |  |
| `wrap_row(row)` | The grid with a one-cell border copied from the opposite edge. |
| `pad_toroidal(grid)` |  |
| `crop_border(grid)` |  |
| `life_step_toroidal(grid)` | The interior of the padded grid sees exactly the toroidal neighbourhood, so |
| `life_after_toroidal(grid, gens)` |  |
| `neighbours_toroidal(grid, r, c)` |  |
| `is_oscillator_toroidal(grid, max_period)` |  |
| `seed_row(width)` | A row of `width` cells with the middle one alive. Every published elementary |
| `elementary_from_seed(rule, width, gens)` |  |
| `rule_catalog()` | The elementary rules with an established character. The catalog is data, not |
| `rule_name(rule)` |  |
| `density(grid)` | Live fraction of the frame. Beware the runtime's division: an evenly divided |
| `diff_grid(a, b)` | Where two generations disagree, as a grid — so population, live_coords and |
| `generation_diff(a, b)` |  |
| `changed_coords(a, b)` |  |
| `render_cell(v)` |  |
| `render_row(row)` |  |
| `render_grid(grid)` |  |
| `neighbour_offsets()` | The eight offsets, as data. Life's `neighbours` writes them out longhand and |
| `neighbours_equal(grid, r, c, v)` | Neighbours in state v. Outside the frame reads as 0, so asking for v == 0 |
| `seeds_cell(grid, r, c)` | Seeds — B2/S: born on exactly two neighbours, and NOTHING ever survives. |
| `seeds_step(grid)` |  |
| `brain_cell(grid, r, c)` | Brian's Brain — three states: 0 off, 1 firing, 2 refractory. A firing cell |
| `brain_step(grid)` |  |

## shinsuu

`shinsuu` 0.1.0 · needs: retsu

shinsuu (進数) — base conversion and checksums.

| definition | what it does |
|---|---|
| `digits()` |  |
| `to_base(n, base)` |  |
| `to_base_digits(n, base)` |  |
| `to_hex(n)` |  |
| `to_binary(n)` |  |
| `to_octal(n)` |  |
| `digit_value(ch)` |  |
| `value_index(xs, v, i)` |  |
| `from_base(text, base)` |  |
| `digits_of(n)` | Digits of a number in base 10, most significant first. |
| `digit_sum(n)` |  |
| `luhn_valid(n)` | The Luhn checksum, which every credit-card number satisfies. |
| `luhn_digit(d, i)` |  |
| `base_digits(n, base)` |  |
| `from_digits(ds, base)` | The inverse of base_digits. Horner's rule, so no `expt` and no float. |
| `pad_digits(ds, width)` | Left-pad a digit list to a fixed width with zeros. |
| `bits_of(n)` |  |
| `from_bits(bs)` |  |
| `bits_zip(f, a, b)` |  |
| `bits_and(a, b)` |  |
| `bits_or(a, b)` |  |
| `bits_xor(a, b)` |  |
| `bits_not(bs)` |  |
| `bit_and(a, b)` |  |
| `bit_or(a, b)` |  |
| `bit_xor(a, b)` |  |
| `bit_not(n, width)` | NOT takes a width, and that is not an oversight. |
| `bit_at(n, i)` |  |
| `shift_left(n, k)` |  |
| `shift_right(n, k)` |  |
| `popcount(n)` |  |
| `bit_length(n)` | Bits needed to write n, with zero needing none. |
| `is_power_of_two(n)` |  |
| `next_power_of_two(n)` | The smallest power of two that is not less than n. |
| `pad_digits_left(s, width, pad)` |  |
| `to_base_width(n, base, width)` | A base rendering that is exactly `width` characters wide. |
| `to_hex_width(n, width)` |  |
| `to_binary_width(n, width)` |  |
| `digital_root(n)` | Repeated digit sum until one digit is left. |
| `reverse_digits(n)` | The digits read backwards, as a number. |
| `is_palindrome_number(n)` |  |
| `is_palindrome_in_base(n, base)` | The same question asked in another base, where the answer often differs: |
| `gray_encode(n)` | --------------------------------------------------------------------------- |
| `gray_decode(g)` |  |
| `gray_decode_from(acc, shifted)` |  |
| `roman_values()` |  |
| `roman_symbols()` |  |
| `to_roman(n)` | Zero and anything below it render as the empty string. |
| `to_roman_from(n, vs, ss, i)` | The two tables are carried down the recursion rather than re-read from the |
| `roman_value(ch)` |  |
| `roman_value_of(c)` |  |
| `from_roman(text)` | Read a numeral by looking one symbol ahead: a symbol worth less than the one |
| `from_roman_at(cs, i)` | These call roman_value_of, not roman_value: from_roman has already upcased the |
| `roman_next(cs, i)` |  |
| `is_canonical_roman(text)` | Does this text spell exactly the canonical numeral for the number it reads as? |
| `digits36()` |  |
| `digit_value36(ch)` |  |
| `to_base_n(n, base)` |  |
| `to_base_n_digits(n, base)` |  |
| `to_base36(n)` |  |
| `all_digits_in_base(cs, base)` |  |
| `is_valid_in_base(text, base)` | Is every character of `text` a digit of `base`? |
| `parse_int(text, base)` | Parse, answering nil when the text is not a numeral in this base. |
| `parse_signed(cs, base, sign)` |  |
| `from_base36(text)` |  |
| `mod_checksum(n, m)` | The simplest checksum there is: the digit sum, modulo m. |
| `mod_check_digit(n, m)` | The digit to append so that the result checksums to zero. |
| `mod_check_valid(n, m)` |  |
| `luhn_check_digit(n)` | The Luhn digit to append. Note the `i + 1`: appending shifts every existing |
| `luhn_valid_digits(ds)` | Luhn over a digit LIST rather than a number, so a card number with leading |
| `luhn_valid_text(text)` | Luhn on the form a card number is actually written in. Spaces and hyphens are |
| `isbn10_chars(text)` |  |
| `isbn10_char_value(ch)` |  |
| `isbn10_char_ok(ch, i)` |  |
| `isbn10_ok_chars(cs)` |  |
| `isbn10_weighted(cs)` | Weights run 10, 9, 8 … down to 1, so this is also the partial sum over a |
| `isbn10_valid(text)` |  |
| `isbn10_digit_char(v)` |  |
| `isbn10_check_char(first9)` |  |
| `isbn10_complete(first9)` |  |

## shisutemu

`shisutemu` 0.1.0 · needs: retsu, moji

shisutemu (システム) — the system.

| definition | what it does |
|---|---|
| `status_of(cap)` | The exit status of a capture alist; 0 when the shape is wrong or the status |
| `stdout_of(cap)` | The captured stdout; "" when absent or not a capture. |
| `stderr_of(cap)` | The captured stderr; "" when absent or not a capture. |
| `read_or(path, default)` | The file's contents, or `default` when it is not a readable file. |
| `file_age_ms(path)` | Milliseconds since the file was last modified, or nil when it is not a file. |
| `last_nonempty_line(text)` | The last line of `text` that has content in it, or "" when there is none. |
| `esc_char()` | The ESC character, spelled once. Blue strings accept `\u{1b}`. |
| `ansi_final(c)` | True when `c` is the final byte of an ANSI escape sequence: any ASCII letter. |
| `strip_ansi_from(cs, state, acc)` | Walk the characters, dropping every ESC-prefixed sequence. |
| `strip_ansi(s)` | Remove ANSI escape sequences, matching the sentinela widget's strip_ansi. |
| `age_s(ms)` | Compact human age for a millisecond duration — the sentinela display. |
| `short_rev(rev, n)` | A revision shortened to at most `n` characters; the em dash when it is absent. |

## shomei

`shomei` 0.1.0 · needs: retsu

shomei (署名) — signatures and hash chains: Ed25519 and BLAKE3, for real.

| definition | what it does |
|---|---|
| `signing_keypair(seed_hex)` | The Ed25519 keypair for a 32-byte seed (64 hex characters): [secret, public]. |
| `keypair_secret(kp)` | The secret half of a keypair: the seed, in lowercase hex. Never publish it. |
| `keypair_public(kp)` | The public half of a keypair, in lowercase hex. This is what a verifier holds. |
| `sign_message(secret_hex, message)` | The Ed25519 signature of a message (a string or a byte list), in hex. |
| `verify_message(public_hex, message, signature_hex)` | True when the signature is valid for the message under the public key; false |
| `hash_message(data)` | The BLAKE3-256 hash of a string or a byte list, in lowercase hex. |
| `is_hash_hex(s)` | True for exactly 64 lowercase hex characters: the shape `hash_message` returns. |
| `chain_genesis(label)` | The first hash of a chain, from a label that names the log. |
| `chain_link(prev, payload)` | The hash that follows `prev` when `payload` (a string) is appended. |
| `chain_head(genesis, payloads)` | The hash after every payload is linked in order: the genesis for no payloads. |
| `chain_hashes(genesis, payloads)` | Every link hash in order, one per payload, as a log stores them beside its |
| `chain_verify(genesis, payloads, head)` | True when linking the payloads onto the genesis reproduces `head` exactly. |
| `chain_first_break(genesis, payloads, hashes)` | The index of the first stored hash that the payloads do not reproduce, or nil |
| `chain_prev(acc, genesis)` | The most recent link in a reversed accumulator, or the genesis when empty. |
| `chain_at(i, xs)` | The i-th element, or nil past the end, so two lists of different lengths |
| `chain_require_hash(h, what)` | Raise unless `h` has the shape of a hash, naming which argument it was. |

## shuugou

`shuugou` 0.1.0 · needs: ronri, retsu

shuugou (集合) — sets, over plain lists.

| definition | what it does |
|---|---|
| `member(xs, v)` |  |
| `unique(xs)` | Keeps the FIRST occurrence of each element. |
| `union(a, b)` |  |
| `intersection(a, b)` |  |
| `difference(a, b)` |  |
| `subset(a, b)` |  |
| `disjoint(a, b)` |  |
| `symmetric_difference(a, b)` |  |
| `as_set(xs)` | The runtime has TWO empties and only one of them is `[]`: |
| `cardinality(xs)` | Cardinality counts DISTINCT elements, so it is not `size`. |
| `is_singleton(xs)` |  |
| `set_equal(a, b)` | Set equality ignores both order and multiplicity — [1,2,2] and [2,1] are the |
| `superset(a, b)` |  |
| `proper_subset(a, b)` | Proper = contained AND not equal. The case worth stating: no set is a proper |
| `proper_superset(a, b)` |  |
| `insert(xs, v)` | Idempotent by construction: inserting a member returns the set unchanged |
| `remove(xs, v)` | Removes EVERY occurrence, not the first. |
| `unique_by(key, xs)` | unique, but on a derived key — keeps the first element per key, and keeps |
| `group_by(key, xs)` | Returns [key, members] PAIRS rather than a map, in first-seen key order. |
| `frequencies(xs)` | [value, count] pairs, in first-seen order. Same representation as group_by, |
| `lookup(ps, k)` | The value for a key in a pair list, or nil when absent. |
| `most_common(xs)` | The most frequent value; nil for the empty input. |
| `is_set(xs)` | A list is a set exactly when it carries no duplicate — the precondition every |
| `power_set_of(xs)` | The power set of an already-deduplicated list. Split out from `power_set` so |
| `power_set(xs)` | All subsets, INCLUDING the empty set and the set itself. |
| `cartesian_product(a, b)` | All [a, b] pairs with a from the first set and b from the second. ORDERED |
| `pairs(xs)` | Every UNORDERED pair of distinct members: [1,2] appears, [2,1] does not, and |
| `union_all(xss)` |  |
| `intersection_all(xss)` | The intersection of a FAMILY of sets. Seeded with the first member rather |

## tokumei

`tokumei` 0.1.0 · needs: ran, toukei, kazu

tokumei (匿名) — anonymity: learn what a group is without exposing anyone in it.

| definition | what it does |
|---|---|
| `forced_response(truth, p, seed)` | One answer: the truth with probability p, else a fair coin. |
| `forced_response_estimate(yes_share, p)` | The share of true "yes" recovered from the share of "yes" answers: |
| `forced_response_epsilon(p)` | The privacy of one forced-response answer: epsilon = ln((1 + p) / (1 - p)). |
| `forced_response_exposure(prevalence, p)` | How sure an observer is that someone who answered "yes" truly holds the |
| `laplace_draw(scale, seed)` | A draw from Laplace(0, scale), by the inverse CDF. |
| `private_count(count, epsilon, seed)` | A count published with epsilon-differential privacy. |
| `private_share(count, n, epsilon, seed)` | A share of n published with epsilon-differential privacy, kept in [0, 1]. |
| `suppress_small(count, k)` | The count, or nil when it is below the publication threshold k. |
| `unique_share(group_size, cells)` | The chance a person is alone in their cell when a group of `group_size` |
| `replicate_stream(seed, i)` | An independent stream derived from a seed — ran's stream_seed, named here so |

## toukei

`toukei` 0.1.0 · needs: junjo, shuugou

toukei (統計) — descriptive statistics.

| definition | what it does |
|---|---|
| `total(xs)` |  |
| `mean(xs)` |  |
| `median(xs)` |  |
| `spread(xs)` |  |
| `variance(xs)` |  |
| `stddev(xs)` |  |
| `smallest(xs)` |  |
| `largest(xs)` |  |
| `percentile(xs, frac)` | The value below which the given fraction of the data falls. |
| `mean_or_zero(xs)` | The total sibling of `mean`: 0 rather than a 0/0 on the empty list. |
| `sum_of_squares(xs)` | Σ(x - mean)², the numerator every variance shares. |
| `sample_variance(xs)` | The n-1 denominator: the variance of a POPULATION estimated from a SAMPLE. |
| `sample_stddev(xs)` |  |
| `standard_error(xs)` | Spread of the sample MEAN, not of the data: how far the mean itself would |
| `mean_absolute_deviation(xs)` | Mean distance from the mean, without the squaring. |
| `geometric_mean(xs)` | The mean of RATIOS — growth rates, index numbers, anything multiplicative. |
| `harmonic_mean(xs)` | The mean of RATES — speeds, prices per unit, anything "per something". |
| `weighted_mean(xs, ws)` | Weights need not sum to 1 — they are normalised here, so counts, hours and |
| `midrange(xs)` | Midpoint of the extremes. Deliberately ignores everything in between, |
| `coefficient_of_variation(xs)` | Stddev as a fraction of the mean, so spreads measured in different units |
| `median_or_zero(xs)` | median that answers 0 on the empty list instead of indexing off the end. |
| `lower_half(xs)` | The two halves either side of the median, EXCLUDING the median itself when |
| `upper_half(xs)` |  |
| `q1(xs)` | Quartiles as medians of the halves — NOT `percentile(xs, 0.25)`. |
| `q3(xs)` |  |
| `quartiles(xs)` |  |
| `iqr(xs)` | The middle half's width — the spread measure that a single wild point |
| `fences(xs)` | Tukey's 1.5×IQR cut points, returned as [low, high]. |
| `outliers(xs)` | Points outside the fences, in input order. Empty means "nothing unusual", |
| `percentile_rank(x, xs)` | The inverse of `percentile`: what fraction of the data is at or below x. |
| `covariance(xs, ys)` | Population covariance: the mean product of paired deviations. |
| `correlation(xs, ys)` | Pearson r — covariance divided by both spreads, so it is unitless and |
| `z_score(x, xs)` | How many standard deviations x sits from the mean — the transform that |
| `z_scores(xs)` |  |
| `rescale(xs)` | Min-max rescaling onto [0, 1]. |
| `cumulative_sum(xs)` | Running totals, ONE PER INPUT ELEMENT — so the last entry is exactly |
| `moving_average(xs, w)` | Simple moving average, one value per COMPLETE window. |
| `skewness(xs)` | Population skewness: which tail is longer. Positive means the long tail is |
| `excess_kurtosis(xs)` | EXCESS kurtosis — the normal distribution scores 0, not 3. |
| `tally(x, xs)` | How many times x occurs. The counting primitive the three functions below |
| `frequency_table(xs)` | Every distinct value paired with its count, ascending by value. |
| `mode(xs)` | The most frequent value; nil for the empty list. |
| `modes(xs)` | Every value tied for most frequent, ascending. |
| `five_number_summary(xs)` | The five-number summary a boxplot is drawn from, in the order it is drawn: |

## zenbu

`zenbu` 0.1.0 · needs: angou, deeta, gyouretsu, hizuke, junjo, kansuu, kazu, kikagaku, kueri, kumiawase, moji, mokuroku, nisshi, ongaku, raifusaikuru, ran, retsu, ronri, ryouiki, sabi, seimei, shinsuu, shisutemu, shomei, shuugou, tokumei, toukei

zenbu (全部) — the whole distribution, as ONE dependency.

| definition | what it does |
|---|---|
